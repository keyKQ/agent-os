# Console Rewrite Parity Matrix

> **Plan 1 complete — 2026-07-20.** Layer-0/1 foundation (bootstrap, WS-RPC,
> theme, AppShell + 13 routes, health view) ported and verified: FE gate green
> (77 unit tests after this parity-fix round grew the covering suites, `vite
> build` clean), Python gate green (ruff + mypy clean,
> 1559 gateway/parity tests passing), legacy UI byte-identical (empty
> `git diff` on static js/css/vendor + templates). Every cross-cutting row is
> `ported` except two cutover-plan items (`tokenViz` feature flag, custom
> `base_path`) — the theme-flash inline script and noscript message ride in the
> new SPA shell `frontend/index.html` and are `ported`, with only their
> served-page wiring folded into cutover; the `### health` section has
> zero functional `pending` rows (one owner-sign-off `waived` delta at cutover,
> one live-parity row folded into cutover). Remaining `pending` rows and the 12
> unfilled view sections belong to Plan 2+.

Single source of truth for migration completeness (spec §6). A behavior row
may be `pending`, `ported` (with evidence: test name or verification note),
or `waived` (with reason, owner-approved at cutover).

Row format:
| behavior | legacy source | status | evidence / reason |

## Cross-cutting
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| Theme persistence + system-default resolution | js/theme.js:8-38 | ported | theme.test.ts::theme store > initTheme applies stored preference; initTheme resolves the system default (dark) when nothing is stored; initTheme resolves the system default (light) when nothing is stored (both mock matchMedia '(prefers-color-scheme: dark)'); initTheme prefers a stored value over the system default; set persists and applies; toggle flips the mode; rejects invalid modes |
| Theme flash prevention inline script | templates/index.html (head) | ported | frontend/index.html head `<script>` (lines 13-27) runs before CSS: reads localStorage['agentos-theme'], falls back to matchMedia('(prefers-color-scheme: dark)') when unset/invalid, sets `data-theme` on documentElement, catches storage errors → 'light' — 1:1 with legacy templates/index.html:12-24. Served as the SPA shell `<head>`; wiring the built shell as the rendered page is the cutover-plan item, the inline-script behavior itself is ported |
| Favicon links: icon + shortcut icon + apple-touch-icon → agentos-mark.png | templates/index.html (head favicon block) | ported | frontend/index.html head — three link tags reference /src/assets/agentos-mark.png (png copied from static/img); Vite fingerprints the asset and rewrites the href under base_path at build (base '/control/static/dist/'), mirroring legacy's base_path-prefixed hrefs |
| WS handshake: connect.challenge -> connect(protocol 3) -> HelloOk+policy | js/rpc.js:87-127 | ported | ws-rpc.test.ts::handshake > answers connect.challenge with a protocol-3 connect request incl. auth token; enters connected state and stores policy on HelloOk |
| WS req/res correlation + typed errors (code/details) | js/rpc.js:45-147 | ported | ws-rpc.test.ts::call correlation > resolves with payload on ok res, matching by id; rejects with RpcError carrying code and details; rejects immediately when not connected; rejects all pending calls when the socket closes |
| WS event fan-out incl. wildcard '*' listener | js/rpc.js:148-154 | ported | ws-rpc.test.ts::events > fans out to named and wildcard listeners with meta |
| WS seq-gap detection -> close+reconnect (_gap) | js/rpc.js:188-202 | ported | ws-rpc.test.ts::events > detects a seq gap, emits _gap, and closes the socket |
| WS tick-watch (policy.tick_interval_ms, 2.5x timeout) | js/rpc.js:204-217 | ported | ws-rpc.test.ts::keepalive and reconnect > closes the socket when no frame arrives within the tick timeout |
| WS keepalive ping every 55s | js/rpc.js:172-179 | ported | ws-rpc.test.ts::keepalive and reconnect > sends a ping every 55s while open |
| WS reconnect backoff 800ms x1.7 max 15s | js/rpc.js:226-231 | ported | ws-rpc.test.ts::keepalive and reconnect > reconnects with backoff after close (800ms first retry) |
| Default route: /overview desktop, /chat on <=768px | js/router.js:32 | ported | app/routes.tsx::defaultViewPath + IndexView (index route renders the default view in place); matchMedia('(max-width: 768px)') → chat else overview; AppShell.test.tsx::index route renders the default view without changing the URL > renders Overview on desktop…; renders Chat on mobile… |
| Index route renders the default view WITHOUT rewriting the URL, and highlights the default view's nav item | js/router.js:29-66 | ported | app/routes.tsx IndexView renders the default view's element at the base path (no Navigate/replace, address bar stays at base); AppShell.tsx activePath forces the default nav item .is-active + aria-current="page" at the index; AppShell.test.tsx::renders Overview on desktop and leaves the URL at "/" (asserts router pathname "/" + aria-current on the Overview link) |
| ^ (was) delta: matchMedia evaluated once at module load vs legacy per-navigation | routes.tsx defaultViewPath vs js/router.js:32 | resolved | RESTORED to per-resolve: defaultViewPath() reads matchMedia inside IndexView render (and AppShell activePath), not once at module load — matches legacy's per-`_resolve()` evaluation; earlier waiver retired |
| 404 route fallback rendered as text (XSS-safe) | js/router.js:48-55 | ported | app/routes.tsx::NotFound reads useLocation().pathname (router-driven — createMemoryRouter never updates window.location, so the earlier window.location read made the XSS assertion vacuous) and renders it as JSX text; AppShell.test.tsx::routes > renders XSS-safe 404 text for unknown paths now routes to '/nope<script>alert(1)</script>' and asserts the full hostile string is present as literal text AND no <script> element was injected |
| ^ (was) delta: new 404 renders full pathname (/control/nope) vs legacy basename-relative (/nope) | routes.tsx NotFound vs js/router.js:54 | resolved | RESTORED to basename-relative: NotFound reads useLocation().pathname, which is basename-relative under react-router (main.tsx sets basename from BASE_URL), so it shows '/nope' like legacy's `rel` — not the full '/control/nope'; earlier waiver superseded |
| Document title per route ("<Title> - AgentOS Control"; unmatched route -> "Not Found - AgentOS Control") | js/router.js:68-71 | ported | AppShell.test.tsx::routes > sets the document title from the route (views/StubView.tsx useEffect); sets the 404 document title to "Not Found - AgentOS Control" (routes.tsx NotFound useEffect — an unmatched route has no meta.title so legacy resolves to 'Not Found', router.js:68) |
| Nav active state + aria-current | js/router.js:59-66 | ported | app/AppShell.tsx computes activePath (current pathname segment, or defaultViewPath() at the index) and sets .is-active styling + aria-current="page" on the matching Link — including the index case where no URL segment exists; AppShell.test.tsx::renders Overview on desktop… / renders Chat on mobile… assert aria-current on the default nav item |
| Bootstrap data: version/ws_url/auth_mode/base_path/config_path/features | control_ui.py:_build_bootstrap_context | ported | test_control_ui_bootstrap.py::test_bootstrap_returns_json_context |
| Bootstrap consumption: fetch /api/bootstrap, connect WS (stored wsUrl/wsToken override), mirror _state into connection store | js/app.js (bootstrap fetch + ws connect) | ported | lib/bootstrap.ts + app/providers.tsx; the fetch→WS-connect path is exercised directly (not just indirectly via view tests): providers.test.tsx stubs fetch and asserts rpc.connect args on the resolved bootstrap — ::reads the auth token from sessionStorage, not localStorage; connects without a token when sessionStorage has none; prefers the stored wsUrl override over bootstrap ws_url. _state mirroring into the connection store is wired via rpc.on('_state', …) in providers.tsx |
| ^ delta: legacy inlined bootstrap in the HTML (could not fail); new fetch of /api/bootstrap can. On failure the shell still renders and connects with the location-derived default WS URL (fallbackBootstrap mirrors app.js:186-203 + getDefaultRpcUrl) | js/app.js:186-203 | ported | providers.test.tsx::renders the shell and connects with the location default when bootstrap fetch rejects; …when bootstrap responds non-ok; lib/bootstrap.ts fallbackBootstrap/defaultWsUrl |
| Stored WS override wins over bootstrap ws_url (agentos.wsUrl / agentos.wsToken) | js/app.js:197-203 | ported | app/providers.tsx — URL override read from localStorage['agentos.wsUrl'] (used verbatim, ahead of resolveWsUrl), auth token read from sessionStorage['agentos.wsToken'] (legacy per-tab tier, app.js:201/:212), both try/catch-guarded; providers.test.tsx::reads the auth token from sessionStorage, not localStorage; connects without a token when sessionStorage has none; prefers the stored wsUrl override |
| ^ delta: default WS scheme. Legacy always derived the scheme from location.protocol (wss:// on https, app.js:191-195); the new console prefers the server-computed bootstrap ws_url. Behind a TLS-terminating proxy that omits x-forwarded-proto the server emits ws:// for an https page — a mixed-content downgrade the browser blocks. RESTORED to legacy: resolveWsUrl (bootstrap.ts) prefers the location-derived wss:// default when the page is https and ws_url is a same-host ws:// downgrade; a different host, an already-wss ws_url, or a non-https page passes through untouched. Stored overrides are unaffected (used verbatim). | js/app.js:191-195 getDefaultRpcUrl | ported | lib/bootstrap.ts resolveWsUrl; bootstrap.test.ts::resolveWsUrl (same-host downgrade → wss; wss passthrough; cross-host ws left alone; http page untouched; genuinely-unparseable 'ws://[bad' exercises the catch branch; same-host ws on http page proves the early return); providers.test.tsx::connects with the location-derived wss default when bootstrap ws_url downgrades a same-host https page to ws:// |
| Mobile sidebar drawer (<=768px): hamburger toggle, close on nav-click / outside-click / Escape, aria-expanded + aria-hidden/inert sync | js/app.js:119-171 | ported | AppShell.test.tsx::mobile sidebar drawer > hides the drawer on mobile until the hamburger opens it, and closes on nav click; closes on Escape and on outside click; keeps the sidebar visible to AT on desktop; app/AppShell.tsx (matchMedia 768px + addListener fallback, document click/keydown listeners, inert/aria-hidden when hidden drawer) |
| Connection indicator: PERSISTENT pill (never unmounts), variant ok/warn/err + compact 'Connected' ok state, capitalized label as text + title attr, role=status aria-live=polite | js/app.js:94,174-183 | ported | AppShell.tsx renders a persistent #conn-pill in topbar-left across all states (connected→ok, connecting→warn, disconnected→err; label = capitalized state; title=label); AppShell.test.tsx::shows a persistent connection pill across all states including Connected (asserts the pill STAYS mounted with the ok/'Connected' state, title, and data-variant on connect — legacy did not unmount the indicator) |
| Sidebar version footer: 'v<semver>' from bootstrap.version with build-suffix (+NNN) stripped + safe-charset filtered (max 32 chars), suppressed entirely when empty | js/app.js:58-68 (_buildLayout version footer) | ported | AppShell.tsx sidebarVersion() (split('+')[0], /[^0-9A-Za-z.\-]/ filter, slice(0,32)) + nav-foot block reading useBootstrap().version; AppShell.test.tsx::renders the sidebar version footer with the build-suffix stripped (2026.7.19+1779915602 → v2026.7.19); suppresses the version footer when the bootstrap version is empty |
| Sidebar information architecture: nav grouped under labels Chat / Control / Settings, Chat first, Approvals last under Settings; item order within groups matches legacy markup | js/app.js:72-88 (_buildLayout nav structure) | ported | AppShell.tsx NAV_GROUPS (Chat: chat; Control: overview,health,channels,skills,sessions,agents,usage,cron; Settings: setup,config,logs,approvals); AppShell.test.tsx::groups nav under Chat / Control / Settings with Chat first and Approvals last (asserts label order + first link Chat / last link Approvals) |
| noscript message | templates/index.html | ported | frontend/index.html body `<noscript>` (lines 30-35): "JavaScript required" heading + the "needs JavaScript to render the chat, sessions, and configuration views" sentence, mirroring legacy templates/index.html:47-52 (new drops the legacy inline light-only border/color styling, which is theme-polish deferred to the styling pass, not a behavior). Served in the SPA shell; cutover wires the built shell as the rendered page |
| Feature flag AGENTOS_FEATURES.tokenViz (default false) | js/app.js:6-9 | pending | |
| Custom base_path support for built assets | control_ui.py + vite base | pending | cutover-plan item |

## Views
(One section per view; filled by each view's Task before implementation.
 Health is filled in this plan; the other 12 in later plans.)

### health
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| doctor.status RPC {agentId:'main', deep:true} after waitForConnection | health.js:76-77 | ported | HealthPage.test.tsx::HealthPage > calls doctor.status deep for agent main and renders grouped findings (asserts call('doctor.status', {agentId:'main', deep:true}) after waitForConnection) |
| doctor.status call lifecycle: exactly one deep call per view entry, error rendered immediately (no retry), fresh load on re-entry, no background refetch on tab focus / reconnect | health.js:64-77 | ported | HealthPage.tsx useQuery pinned to legacy (retry:false, staleTime:0, gcTime:0, refetchOnMount:'always', refetchOnWindowFocus/Reconnect:false — overrides providers.tsx defaults staleTime 5s / retry 1); HealthPage.test.tsx::renders the error immediately without retrying; reloads fresh on every view entry instead of serving a cached report |
| Loading state: "Checking readiness" + loading strip, and every (re)load resets to it BEFORE the deep call settles | health.js:64-74 | ported | HealthPage.tsx gates the view on query.isFetching (not just the pre-data branch): Refresh and every fresh view entry blank the stale report to the loading placeholders immediately, matching legacy _load resetting summary → "Checking readiness", rail → is-loading strip, findings → "Loading health report" at the top of _load before doctor.status resolves. HealthPage.test.tsx::resets to the loading state on Refresh before the refetch settles; reloads fresh on every view entry instead of serving a cached report. Loading visual polish → manual dev-loop check |
| Success: summary text, status rail class is-<status>, impact count tiles | health.js:80-84,133-150 | ported | HealthPage.test.tsx::HealthPage > calls doctor.status deep… (renders 'Ready with warnings' + summary); HealthPage.tsx StatusRail/CountTile |
| Fallback impactCounts derived from severity counts | health.js:413-420 | ported | logic.test.ts::impactCountsFromSeverity > maps severity counts to impact counts; HealthPage.tsx StatusRail uses impactCounts ?? impactCountsFromSeverity |
| Findings grouped: action/degraded/optional/ready with notes | health.js:277-313 | ported | HealthPage.test.tsx > …renders grouped findings (asserts 'Degraded capabilities' group + note); HealthPage.tsx FindingsSection/GROUPS |
| Impact derivation: readinessImpact else severity mapping | health.js:403-411 | ported | logic.test.ts::impactValue > passes through valid readinessImpact; maps severity error/warn/info/ok |
| Status labels incl. "Ready with warnings" for ready+degraded | health.js:462-472 | ported | logic.test.ts::statusLabel > "Ready with warnings" when ready but degraded; maps action_required |
| Finding card: severity/impact/surface meta, badges (.diagnostic.incomplete, .repair.pending, config.mismatch), restartRequired chip | health.js:324-368 | ported | HealthPage.test.tsx > …renders 'Memory is slow' finding; HealthPage.tsx FindingCard/findingBadge (meta+badges+restart chip); badge/chip variants → manual dev-loop check |
| Evidence tags: max 6, hidden keys restart_required/restartRequired, camelCase->label, JSON values truncated 120 | health.js:439-460,474-483 | ported | logic.test.ts::evidence > hides restart keys and null values; labels camelCase keys keeping each hump capitalized (gatewayUrl -> "Gateway Url", 1:1 with legacy — the earlier `.toLowerCase()` "Gateway url" divergence is removed); labels snake_case keys; truncates long JSON at 120; HealthPage.tsx EvidenceTags slices to 6 |
| Fix steps: numbered, optional command with copy button, heading by kind | health.js:370-401 | ported | HealthPage.test.tsx > …renders 'agentos gateway restart' command; HealthPage.tsx StepsList/CommandRow/stepsHeading |
| Copy command: navigator.clipboard w/ execCommand fallback + ok/err toast | health.js:35-62 + components.js UI.toast | ported (delta) | HealthPage.tsx copyText (clipboard + execCommand fallback) + onCopyCommand: toast.success('Copied command', {id:'health-copy-ok', duration:1600}) / toast.error('Copy failed: …', {id:'health-copy-err', duration:2500}) — matches legacy UI.toast durations (1600ms ok / 2500ms err) and message-dedupe (stable per-outcome id collapses repeats into one toast; legacy keyed by `${type}\0${message}`). HealthPage.test.tsx::copy success fires a 1600ms ok toast with a stable id; copy failure fires a 2500ms err toast with a stable id; re-copying reuses the same ok toast id so identical toasts dedupe. **Residual sonner-seam divergence:** sonner 2.0.7 renders the whole toast list in ONE aria-live="polite" `<section>` and dropped the per-toast `important` option (present in older sonner), so error toasts are NOT announced assertively (role="alert" / aria-live="assertive") the way legacy UI.toast set role="alert" on err/warn toasts — not achievable through the sonner API at this version, revisit if sonner re-adds an assertive path or at cutover. |
| Error state: synthetic gateway.unavailable finding w/ local-vs-remote fix steps, shell-quoted commands | health.js:86-115,191-268 | ported (delta) | HealthPage.test.tsx::HealthPage > renders the synthetic gateway.unavailable finding on RPC failure; shows "Gateway health report unavailable" in the readiness rail, not the raw status token; logic.test.ts::shellArg + gateway url helpers (isLocalGatewayUrl/gatewayStatusTarget); HealthPage.tsx error branch + logic.gatewayUnavailableFixSteps. **Divergence history (now fixed):** an earlier port left the synthetic error report's rail `summary` blank so the readiness rail fell back to the raw "unavailable" status token; restored to legacy behavior (health.js:92-95) — the rail summary now carries the "Gateway health report unavailable" sentence, while the header #health-summary line keeps the distinct "Health report unavailable" (health.js:89). The covering test that had mandated the blank rail summary was updated to assert the sentence. |
| Error-path usesDefault predicate: URL-equality vs the default RPC URL (protocol+host+pathname, query/hash ignored; empty URL falls back true, unparsable/unknown-default false) — NOT mere absence of the localStorage override, since legacy saveConnectionSettings stores the default URL itself (app.js:210) | health.js:227-238 | ported | logic.ts usesDefaultGatewayUrl (bootstrap.ws_url stands in for App.getDefaultRpcUrl()); logic.test.ts::usesDefaultGatewayUrl (5 cases); HealthPage.test.tsx::uses config-target fix steps when the stored wsUrl equals the default; uses gateway-target fix steps when the stored wsUrl differs from the default |
| ^ delta: error-path Config row gated on usesDefault AND isLocalGatewayUrl (legacy gated on usesDefault only) — remote-default deployments no longer show a local configPath in the error evidence/rail; judged more-correct, revisit at cutover if strict parity required | HealthPage.tsx error branch vs health.js:88 | waived (review Task 8) | reviewer finding, owner sign-off at cutover gate; waiver covers ONLY the added isLocalGatewayUrl AND-gate — usesDefault itself follows legacy URL-equality (row above) |
| Refresh button re-runs the report | health.js:17-24 | ported | HealthPage.test.tsx::HealthPage > refetches when Refresh is clicked (click → 2nd doctor.status call); HealthPage.tsx Refresh Button onClick=refetch |
| _gatewayContextUrl() → localStorage['agentos.wsUrl'] \|\| bootstrap.ws_url | health.js:172-185 | ported (simplified) | Legacy read App.loadConnectionSettings().url; new impl reads localStorage['agentos.wsUrl'] ?? bootstrap.ws_url — same effective value. HealthPage.tsx gatewayUrl; exercised via mocked useBootstrap in HealthPage.test.tsx |
| Live parity: /health vs legacy /control/health side-by-side | health.js (whole view) | RTL + manual pending | Live gateway check infeasible: running gateway on :18791 is a stale wheel (2026.7.18.post1, serves index HTML not current JSON contract) and holds the shared ~/.agentos state lock, so a fresh worktree gateway on :18999 refuses to start (pid 8228 owns state_dir); not stopped (user process). Behaviors covered by 12 HealthPage RTL + 22 logic unit tests (grown by this round's health-parity batch — copy toasts, loading-reset, error-rail, usesDefault, evidence casing); visual parity pending a clean gateway |

| Health view visual styling (health-* classes) | css/views/health.css | ported (restyled) | styled in views/health/health.css on the shared tactical token system (globals.css ports legacy base.css tokens verbatim: near-black ground, hairline tiles, lime signal, mono labels); visually verified in both themes via browser |

### approval-monitor
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| REST endpoint: GET /api/approvals is ROOT-absolute (NOT under control_ui.base_path, unlike /api/bootstrap) — the routes register at the gateway root and the rate-limit exemption keys off the bare path; must not be rewritten through the BASE_URL base | approval_monitor.js:67 + app.py:535-537 + middleware.py:240 | ported | services/approval-monitor.ts approvalsUrl()/approvalsResolveUrl() return '/api/approvals' + '/api/approvals/resolve' verbatim; approval-monitor.test.ts::approvalsUrl / approvalsResolveUrl are root-absolute (not base-path rewritten); polls the root-absolute /api/approvals immediately on start. **Live-verified:** curl http://127.0.0.1:18999/api/approvals → HTTP 200 {"pending":[],"mode":"prompt","allowPatterns":[],"denyPatterns":[]} — exact response contract the service consumes |
| Adaptive poll backoff: start 1500ms, ×2 on empty/error, clamp 30000ms, reset to 1500ms on pending | approval_monitor.js:4-5,38-54,63-98 | ported | services/approval-monitor.ts POLL_MS/POLL_MAX_MS + schedulePoll/resetPollBackoff/increasePollBackoff; approval-monitor.test.ts::polls immediately on start (delay 0); grows the backoff 1500→3000→6000…; clamps the backoff at 30000ms (does not grow without bound); resets the backoff to 1500ms once the queue has pending items |
| Self-rescheduling poll loop with reentrancy guard (pollBusy) | approval_monitor.js:38-46,63-66 | ported | services/approval-monitor.ts schedulePoll re-arms after each poll; pollBusy guard in poll(); covered indirectly by the backoff-cadence tests (a leaked/duplicated tick would break the exact-count assertions) |
| Re-poll immediately + reset backoff on window focus | approval_monitor.js:107-110 | ported | services/approval-monitor.ts onFocus; approval-monitor.test.ts::re-polls immediately and resets backoff on window focus; detaches focus/visibility listeners on stop() |
| Re-poll immediately + reset backoff on document visibilitychange (visible only) | approval_monitor.js:100-105 | ported | services/approval-monitor.ts onVisibilityChange; approval-monitor.test.ts::re-polls when the document becomes visible; does not re-poll when the document becomes hidden |
| Publish pending list + count to consumers (was CustomEvent 'agentos:approvals-pending' + imperative badge DOM writes; now zustand useApprovals store) | approval_monitor.js:112-138 | ported (delta) | services/approval-monitor.ts useApprovals store (pending/count/mode); approval-monitor.test.ts::publishes pending + count to useApprovals on a successful poll. **Delta:** legacy fanned out via window CustomEvent and wrote #approval-count/#approval-inline directly; the React port replaces both with a reactive store read by AppShell (badge) + ApprovalPrompt (modal). The legacy #approval-inline banner is folded into the nav badge (single surface); revisit at cutover if a separate inline banner is required |
| Transient poll failure (non-ok / thrown) zeroes ONLY the badge and NEVER dismisses an open prompt | approval_monitor.js:71-74,92-97 (error branches call _setBadge(0), never touch _modal) | ported | The store deliberately decouples `count` (badge) from `pending` (modal): the poll error/catch branches call useApprovals.zeroBadge() (count → 0, pending intact), not setFromPoll([]). approval-monitor.test.ts::zeroes the badge but PRESERVES pending on a non-ok response (open prompt survives); zeroes the badge but PRESERVES pending when fetch throws. ApprovalPrompt.test.tsx::keeps an open prompt when a transient poll failure zeroes the badge (finding 1). **Reviewer finding 1 fix** (earlier port called setFromPoll([]) on failure → unmounted the open prompt mid-read) |
| Modal PINNED to the item captured at open time; the displayed approval never silently swaps under the operator while the prompt is open | approval_monitor.js:90-91 (`if (_modal) return` short-circuits polls while open) | ported (delta) | ApprovalPrompt.tsx holds the shown item in useState and only (re)assigns it when no prompt is open; while open, a changing queue head leaves the shown item untouched until it is resolved or leaves the queue (matched by id), then advances to the new head. ApprovalPrompt.test.tsx::does NOT swap the displayed item when the queue head changes while open (finding 2); advances to the new head once the pinned item leaves the queue; closes when the pinned item is resolved and the queue empties. **Delta / reviewer finding 2 fix:** legacy skipped polls entirely while the modal was open; the port keeps polling (badge stays live) but pins the modal item in React state — same operator-facing invariant (shown item is stable), without freezing the badge. Earlier port recomputed pending[0] every render → the visible item could swap and busy-state desync |
| Toast on new pending count (once per distinct count; reset when queue empties) | approval_monitor.js:83-88 | ported (delta) | services/approval-monitor.ts poll() lastToastCount gate → toast.warning('Approval required', {duration:2500}); approval-monitor.test.ts::toasts once when a new pending count appears, and not again for the same count; re-toasts when the count changes after dropping to zero. **Delta:** legacy UI.toast(msg,'warn',2500) → sonner toast.warning with a stable id (same 2500ms duration; sonner assertive-announce seam limitation recorded on the health copy-toast row applies) |
| POST /api/approvals/resolve with action→(approved, allowAlways, rememberIntent, elevatedMode) mapping | approval_monitor.js:171-179,186-197 | ported | services/approval-monitor.ts resolve() action map; approval-monitor.test.ts::POSTs approve-once and re-polls; maps "always" to allowAlways + rememberIntent; maps "bypass" to elevatedMode…; maps "deny" to approved:false; defaults namespace to exec when the item omits it |
| Resolve reentrancy guard (busy) + re-poll after resolve + error toast on failure | approval_monitor.js:186-220 | ported (delta) | services/approval-monitor.ts resolve() busy guard, re-poll on success, throws on HTTP error so the modal re-enables buttons; approval-monitor.test.ts::rejects and error-toasts on a non-ok resolve, without persisting elevated mode. **Delta:** legacy re-polled via setTimeout(_poll,150); the port awaits poll() immediately (no DOM settle to wait on — the store drives the modal) |
| Modal closes on resolve SUCCESS before the re-poll (pin released explicitly, not via the store draining) | approval_monitor.js:206 (_closeModal() runs on success, before the 150ms _poll) | ported | ApprovalPrompt.tsx resolve() calls setItem(null) on resolve success (before the store re-poll settles), so the resolved item can never flash while the re-poll is in flight; on FAILURE the pin is kept so the operator retries the same approval. ApprovalPrompt.test.tsx::releases the pin on resolve success even before the store updates (finding 3); keeps the pinned item on resolve FAILURE so the operator can retry. **Reviewer finding 3 fix** (earlier port relied on the store update alone) |
| Outcome toast: approve granted (info) / deny (warn) / bypass enabled (warn) | approval_monitor.js:207-211 | ported | services/approval-monitor.ts resolve() → toast.success('Approval granted') / toast.warning('Approval denied') / toast.warning('Approval bypass enabled'); approval-monitor.test.ts covers each branch |
| elevatedMode persistence: localStorage agentos.elevatedMode + agentos.elevatedMode.version='2'; normalize on/bypass/full else clear | approval_monitor.js:6-8,227-239 | ported | services/approval-monitor.ts setBrowserElevated + ELEVATED_MODE_* constants; approval-monitor.test.ts::maps "bypass" to elevatedMode + persists it under storage version 2; setBrowserElevated persists valid modes / clears the keys for an invalid mode. **Delta (CustomEvent → store, consumer now exists):** legacy fanned the elevated-mode change out via window CustomEvent 'agentos:elevated-mode' AND re-ran _loadData after resolve so the approvals readout refreshed. The React port replaces the CustomEvent with a reactive `elevatedMode` slice on the useApprovals store: setBrowserElevated updates the store in lockstep with localStorage (hydrated on load via readBrowserElevated with the storage-version downgrade), and the approvals view's "Effective execution mode" readout subscribes to it — so an in-view Bypass resolve refreshes the readout live, with NO remount. The CustomEvent stays un-dispatched; the store path replaces it. approval-monitor.test.ts::setBrowserElevated mirrors the normalized mode into the reactive store slice; readBrowserElevated (downgrade + unset). ApprovalsPage.test.tsx::updates the effective-execution-mode readout in-view after a Bypass resolve (no remount) |
| Auth header: Bearer token from sessionStorage['agentos.wsToken'] (legacy App.getAuthToken) | approval_monitor.js:56-61 + app.js:205-213 | ported | services/approval-monitor.ts authHeaders(); approval-monitor.test.ts::attaches a Bearer token from sessionStorage when present; omits the Authorization header when no token is stored |
| pollNow() re-poll hook (consumed by the approvals view) | approval_monitor.js:34-36 | ported | services/approval-monitor.ts pollNow(); approval-monitor.test.ts::polls on demand without waiting for the scheduled tick (the approvals view that consumes it lands in a later task) |
| start()/stop() lifecycle: begin polling + wire focus/visibility on start; clear timer, detach listeners, clear store on stop | approval_monitor.js:17-32 | ported | services/approval-monitor.ts start/stop; approval-monitor.test.ts::stop() clears the scheduled timer so no further polls fire; stop() clears the store; detaches focus/visibility listeners on stop(). Wired in app/providers.tsx (start on mount, stop on unmount); providers.test.tsx::starts the approval monitor on mount and stops it on unmount |
| Modal prompt for pending[0] (approve once / always [exec+command only] / bypass / deny), tool label, meta line, command, detail JSON | approval_monitor.js:90-91,140-184,241-258 | ported (delta) | components/ApprovalPrompt.tsx (React port of _openModal/_resolve); ApprovalPrompt.test.tsx::renders the first pending approval as an alertdialog…; prompts only the FIRST pending item; shows "Always Allow This Type" only for exec items with a command; falls back to actionKind / "Tool execution"; renders the detail JSON block when no command is present; each button resolves with its action; disables all buttons while a resolve is in flight. **Delta:** rebuilt on the design-system token system (warn-toned .panel over a dimmed backdrop, command via common <CommandLine>, role="alertdialog") instead of the legacy innerHTML .modal-backdrop/.approval-modal markup; the legacy _esc() manual HTML escaping is obsolete (React escapes text nodes). Modal visual styling in components/ApprovalPrompt.css |
| Pending-count badge on the Approvals nav item (legacy #approval-count, hidden at 0) | approval_monitor.js:118-124 | ported | app/AppShell.tsx approval-count badge reading useApprovals().count on the Approvals nav Link (warn-toned, hidden at 0, singular/plural aria-label); AppShell.test.tsx::hides the badge when no approvals are pending; shows the pending count on the Approvals nav item; uses the singular aria-label for a single pending approval |
| Modal helper functions (approvalCommand/approvalDetail/approvalMeta/canAlwaysAllow) | approval_monitor.js:145-152,241-258 | ported | services/approval-monitor.ts pure helpers; approval-monitor.test.ts::approval-monitor pure helpers (approvalCommand 4 cases, approvalDetail incl. 900-char truncation, approvalMeta, canAlwaysAllow) |
| Modal a11y: focus trap + Escape-to-dismiss | (legacy had none — plain innerHTML .modal-backdrop with no focus trap or Escape handler) | waived (a11y gap, legacy parity) | components/ApprovalPrompt.tsx sets role="alertdialog" + aria-modal="true" and moves focus into the dialog on open, but does NOT trap focus or handle Escape — matching legacy (which offered neither). aria-modal PROMISES modal focus semantics that are not fully delivered, so this is a known a11y gap held at legacy parity, not a regression. Deferred: add a focus trap + Escape (or drop aria-modal) in a later a11y pass. **Reviewer finding 4** (matrix note only, no code this round) |
| Live parity: approval prompt vs legacy /control approval modal | approval_monitor.js (whole service) | RTL + live-endpoint verified, manual modal pending | GET /api/approvals live-verified on :18999 (HTTP 200, exact JSON contract). Visual modal parity pending a queued approval to trigger the prompt (queue was empty at check time); behaviors covered by 38 service + 17 modal + 3 badge + 1 provider-wiring tests (modal grew by the reviewer's pinning + poll-resilience batch). Visual modal styling verified structurally (tokens); pixel parity deferred to cutover manual pass |

### approvals
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| Durable pending list + strategy `mode` sourced from the Task-1 monitor store (was GET /api/approvals in-view) instead of a per-view REST fetch | approvals.js:69-81 | ported (delta) | ApprovalsPage.tsx reads useApprovals().pending + .mode (published by the always-running approvalMonitor); ApprovalsPage.test.tsx::renders the header, strategy radiogroup, and pending count from the store; reflects the active strategy from the store (auto-approve preselected). **Delta:** legacy fetched /api/approvals directly in _loadData and also on a 5000ms setInterval; the console centralizes the poll in the monitor service (adaptive 1500→30000ms backoff + focus/visibility re-poll, approval-monitor.ts) so the view consumes the shared store. `allowPatterns`/`denyPatterns` from the response are unused by the view (legacy read them but never rendered them). |
| Re-poll the monitor on view entry so a stale pending list is refreshed | approvals.js:47 (_loadData on render) | ported (delta) | ApprovalsPage.tsx useEffect → approvalMonitor.pollNow() on mount; ApprovalsPage.test.tsx::re-polls the monitor on mount so the durable pending list is fresh. **Delta:** legacy also re-loaded every 5000ms via setInterval; the shared monitor already runs the adaptive poll loop app-wide, so the view only forces one immediate refresh on entry rather than owning its own interval |
| Effective execution mode: config.get global `permissions.default_mode` combined with the browser session elevated mode | approvals.js:176-216 | ported | ApprovalsPage.tsx useQuery('config.get') after waitForConnection() → resolveExecutionMode(elevatedMode, permissions.default_mode) where elevatedMode is the reactive useApprovals store slice (not a bare localStorage read); logic.test.ts::resolveExecutionMode (session wins / global fallback / neutral fallback / invalid global ignored); executionModeSummary (label + scope-specific blurbs); ApprovalsPage.test.tsx::reads config.get for the effective execution mode after waitForConnection (Global BYPASS surfaces). Legacy 1000ms waitForConnection timeout dropped — react-query already renders the neutral fallback until config.get settles |
| Effective-execution-mode readout refreshes IN-VIEW after an in-view Bypass resolve (legacy re-ran _loadData after resolve) | approvals.js:159-171 (resolve→_setBrowserElevated→_loadData) | ported (delta) | The readout subscribes to the reactive useApprovals `elevatedMode` slice; monitor.resolve('bypass')→setBrowserElevated updates that slice in lockstep with localStorage, so the readout re-renders live with no remount (reviewer gap fix). ApprovalsPage.test.tsx::updates the effective-execution-mode readout in-view after a Bypass resolve (no remount). **Delta:** legacy fanned out via a CustomEvent + a post-resolve _loadData; the store slice replaces both (see the approval-monitor elevatedMode-persistence row — CustomEvent retired, store consumer now exists) |
| Stat row: Pending count, active Strategy label/desc, Effective execution mode | approvals.js:89-106 | ported | ApprovalsPage.tsx ap-stats (Pending from store length, Strategy from activeModeOption, Effective mode from resolveExecutionMode); logic.test.ts::activeModeOption (active else prompt fallback); ApprovalsPage.test.tsx pending count + strategy assertions |
| Approval-strategy radiogroup: prompt / auto-approve / auto-deny with labels + descriptions, active option preselected | approvals.js:82-125 | ported | logic.ts MODE_OPTIONS + activeModeOption; logic.test.ts::MODE_OPTIONS lists prompt/auto-approve/auto-deny in legacy order; ApprovalsPage.tsx role="radiogroup" aria-label="Approval strategy"; ApprovalsPage.test.tsx::renders the header, strategy radiogroup…; reflects the active strategy from the store |
| Save strategy: POST /api/approvals/settings { mode }, then re-poll the monitor (pollNow) + outcome toast; optimistic select reverts on failure | approvals.js:283-303 (incl. ApprovalMonitor.pollNow() at :299) | ported (delta) | services/approval-monitor.ts saveApprovalMode() (root-absolute /api/approvals/settings POST with the session Bearer token, throws on non-ok); approval-monitor.test.ts::saveApprovalMode POSTs { mode } with the session Bearer token; throws on a non-ok response. ApprovalsPage.tsx onSelectMode: optimistic pendingMode → saveApprovalMode → toast.warning + approvalMonitor.pollNow() on success; revert pendingMode + toast.error on failure; ApprovalsPage.test.tsx::saves the strategy on change, toasts, and re-polls; reverts + error-toasts when saving the strategy fails. **Delta:** legacy UI.toast(msg, 'warn'|'info', …) → sonner toast.warning with a stable id (sonner assertive-announce seam limitation, recorded on the health copy-toast row, applies) |
| Pending approval card: toolName (fallback actionKind/"Unknown"), namespace pill, agent + session meta, command block, detail JSON block | approvals.js:249-273,316-332 | ported (delta) | ApprovalsPage.tsx ApprovalCard using the Task-1 pure helpers approvalCommand/approvalDetail (services/approval-monitor.ts) + command via the common <CommandLine>; ApprovalsPage.test.tsx::renders the header… (tool name + command); renders the detail JSON block when a card has no command. **Delta:** legacy _renderApproval built innerHTML with manual _esc(); React escapes text nodes. `awaiting decision` static time label kept (legacy had no real timestamp) |
| Card actions → monitor.resolve: Approve once ('once'), Always allow this type ('always', exec+command only), Bypass approvals ('bypass'), Deny ('deny') | approvals.js:148-171,274-280 | ported | ApprovalsPage.tsx onResolve → approvalMonitor.resolve(item, action) (Task-1 service owns the /api/approvals/resolve POST + action→(approved,allowAlways,rememberIntent,elevatedMode) map + elevatedMode persistence + re-poll); canAlwaysAllow gates the Always button; ApprovalsPage.test.tsx::resolves an approval with "once"; offers "Always allow" only for exec items with a command; resolves with "bypass" and "deny". The legacy in-view resolve fetch + _setBrowserElevated are subsumed by monitor.resolve (approval-monitor row: "POST /api/approvals/resolve…") |
| Empty state when no pending approvals | approvals.js:127-132 | ported | ApprovalsPage.tsx ap-empty ("No pending approvals." + sign-off blurb); ApprovalsPage.test.tsx::shows the empty state when there are no pending approvals |
| Refresh button re-loads approvals | approvals.js:37-46 | ported | ApprovalsPage.tsx Refresh → approvalMonitor.pollNow() + configQuery.refetch(); ApprovalsPage.test.tsx::refreshes the durable list on Refresh |
| Strategy status tone mapping (auto-approve→warn, auto-deny→danger, else ok) | approvals.js:310-314 | ported (delta) | logic.ts modeStateTone; logic.test.ts::modeStateTone (auto-approve→warn / auto-deny→danger / else ok). **Delta:** legacy returned the legacy status-class name 'err' for auto-deny; rebuilt on the design-system `--tone` primitive (danger), applied via tone-* classes on the hero stat + radio tiles — no hardcoded status colors, lime stays signal-only |
| elevatedMode read + storage-version downgrade (localStorage agentos.elevatedMode, legacy 'full' under old version → 'bypass') | approvals.js:234-247 | ported | Single reader services/approval-monitor.ts readBrowserElevated (localStorage + version-downgrade) hydrates the store slice on load; logic.ts browserElevatedMode delegates to it; logic.ts normalizeElevatedMode; logic.test.ts::browserElevatedMode (downgrades legacy full under an old storage version to bypass; keeps full under the current version); normalizeElevatedMode (on/bypass/full pass, else clear); approval-monitor.test.ts::readBrowserElevated (downgrade + unset). Elevated-mode WRITE on 'bypass' resolve is owned by monitor.resolve→setBrowserElevated, which now also updates the reactive store slice (approval-monitor elevatedMode-persistence row) |
| View CSS (ap-* classes) rebuilt on the shared token system | css/views/approvals.css | ported (restyled) | views/approvals/approvals.css on the globals.css token system: header hero + AsciiField backdrop (Health posture), hairline stat tiles with a --tone hero gutter, .panel strategy box + radiogroup, warn-toned pending cards, dashed empty state; status color only via --tone, lime signal-only. Visual polish (both themes, real queued approval) → cutover manual pass |
| Live parity: /approvals vs legacy /control/approvals | approvals.js (whole view) | RTL + build + endpoint verified, manual browser pending | GET /api/approvals live-verified on :18999 (HTTP 200, exact `{pending,mode,allowPatterns,denyPatterns}` contract the store consumes); production `vite build` clean and the view IS in the bundle (grep "No pending approvals"/"Tool execution gate" → index-*.js). Behaviors covered by 12 ApprovalsPage RTL + 20 approvals-logic unit + 3 saveApprovalMode service tests. Browser side-by-side (empty state + a real queued approval card + strategy save) deferred to the cutover manual pass — no queued approval available at check time (queue empty) and no clean worktree gateway+vite pairing was running to drive the SPA |

### logs
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| logs.status RPC {} after waitForConnection → status pills (file log on/off + path, raw turn-call on/off + source/dir, diagnostics standard/raw/off); null status → "Log status unavailable" | logs.js:164-172,262-290 | ported | LogsPage.tsx statusQuery; LogsPage.test.tsx::calls logs.status + logs.tail after waitForConnection and renders status pills; renders "Log status unavailable" when logs.status rejects |
| logs.tail RPC {limit:500, cursor, level:null} polled every 3000ms; cursor advances from data.cursor else += lines.length; reentrancy guard (pollInFlight) | logs.js:135,174-217 | ported | LogsPage.tsx tail poll effect (setInterval 3000, cursor ref, in-flight guard); LogsPage.test.tsx::calls logs.tail with limit 500 + advancing cursor; polls on the 3000ms cadence; does not overlap polls while one is in flight |
| Tail entry normalization: string line → {level:guessLevel, message, raw}; object → level (level\|lvl, upper, default INFO), message (message\|msg\|JSON), ts (timestamp\|ts), raw | logs.js:182-200 | ported | logic.ts normalizeEntry; logic.test.ts::normalizeEntry (string via guessLevel; object level/lvl/default; message/msg/JSON fallback; ts + raw) |
| guessLevel: substring scan ERROR>WARN>INFO>DEBUG>TRACE else INFO | logs.js:219-227 | ported | logic.ts guessLevel; logic.test.ts::guessLevel (each level substring; default INFO) |
| Response line key fallback: data.lines ?? data.entries ?? [] | logs.js:182 | ported | logic.ts extractLines; logic.test.ts::extractLines (lines / entries / empty) |
| Ring buffer: keep only the last 2000 accumulated lines | logs.js:201 | ported | logic.ts clampBuffer; logic.test.ts::clampBuffer (slices to last 2000; passthrough under cap) |
| Level filter chips TRACE/DEBUG/INFO/WARN/ERROR; default active set = DEBUG/INFO/WARN/ERROR (TRACE off); toggle add/remove, aria-pressed | logs.js:10-13,49-55,104-119 | ported | logic.ts LEVELS/DEFAULT_LEVELS; LogsPage.tsx level chips (aria-pressed, toggle); LogsPage.test.tsx::renders default-active level chips (TRACE off); toggling a level chip re-filters the visible lines |
| Search filter: case-insensitive message substring; live re-filter on input | logs.js:121-124,237,296-300 | ported | logic.ts matchesFilter/filterLines; logic.test.ts::filterLines (level+search); LogsPage.test.tsx::filters lines by the search input |
| Line list render: mono, ts sliced to 23 chars (empty→"-"), level chip, message; error/warn lines carry the tone gutter | logs.js:292-323 | ported (delta) | LogsPage.tsx LogLine (t-data mono, tone-danger/tone-warn gutter per level); logic.ts sliceTs; logic.test.ts::sliceTs (23-char slice; empty). **Delta:** legacy per-line innerHTML + manual _esc replaced by React text nodes; the lg-line--error/warn left-rules rebuilt on the shared `--tone` primitive (one gutter per line, error→danger, warn→warn) |
| Search-term highlight: `<mark>` around case-insensitive matches, regex-escaped | logs.js:325-330 | ported (delta) | logic.ts splitHighlight (regex-escaped, returns text/mark segments); logic.test.ts::splitHighlight (match segmentation; escapes regex metachars; empty term passthrough); LogsPage.tsx renders <mark> from segments. **Delta:** legacy built an escaped-HTML string with a replace(); React renders segment nodes so no manual _esc is needed |
| Auto-follow toggle: checked by default; on = scroll display to bottom after each render; re-enabling scrolls to bottom | logs.js:86-90,126-129,322,332-335 | ported | LogsPage.tsx autoFollow state + scrollToBottom effect on lines/toggle; LogsPage.test.tsx::auto-follow scrolls the stream to the bottom on new lines; disabling auto-follow stops the autoscroll |
| Stat row: In-view (filtered count) / of loaded total, Errors, Warnings, Info+Debug counts | logs.js:229-260 | ported | logic.ts countByLevel/visibleCount; logic.test.ts::countByLevel (errors/warns/infos/debug incl TRACE); LogsPage.tsx stat row; LogsPage.test.tsx::stat row shows in-view vs loaded + error/warn counts |
| Loading placeholder ("Loading logs…") before first tail; empty states: "No logs yet." (no lines) / "No lines match the current filter." (filtered empty) | logs.js:94-99,202-207,302-309 | ported | LogsPage.tsx display placeholders; LogsPage.test.tsx::shows the loading placeholder before the first tail; shows "No logs yet." with an empty tail; shows the no-match placeholder when the filter excludes everything |
| Export: build text ("<ts> [LEVEL] message" per filtered line), Blob download agentos-logs.txt | logs.js:337-354 | ported | logic.ts buildExportText; logic.test.ts::buildExportText (ts prefix present/absent; level+message); LogsPage.tsx onExport (Blob + anchor download); LogsPage.test.tsx::export builds the filtered log text and triggers a download |
| Poll error: one-shot warn toast ("Log refresh failed: …"), suppressed until a successful poll clears the flag | logs.js:208-216 | ported (delta) | LogsPage.tsx poll catch → toast.warning (once per error run, pollErrorShown ref), cleared on success; LogsPage.test.tsx::toasts once on a failing tail poll and not again until a poll succeeds. **Delta:** legacy UI.toast(msg,'warn') → sonner toast.warning with a stable id (assertive-announce seam limitation recorded on the health copy-toast row applies) |
| State reset on view (re)entry: cursor 0, buffer empty, search cleared, auto-follow on, default levels | logs.js:39-47 | ported | LogsPage.tsx fresh component state per mount (useState/useRef init) reproduces the legacy render() reset; LogsPage.test.tsx::state assertions exercise fresh mounts |
| View CSS (logs-* classes) rebuilt on the shared token system | css/views/logs.css | ported (restyled) | views/logs/logs.css on the globals.css token system: header hero + AsciiField backdrop (Health posture), hairline stat tiles, .panel toolbar with level chips + search + auto-follow toggle, mono log stream with per-line tone gutters, search-match highlight; status color only via --tone, lime signal-only |
| Live parity: /logs vs legacy /control/logs | logs.js (whole view) | RTL + live-verified | Behaviors covered by 15 LogsPage RTL + 37 logs-logic unit tests. **Live-verified in-browser** against the real worktree gateway on :18999 (vite dev :5173, /control/logs): logs.status populated the pills (File log on / Raw turn-call off / Diagnostics off), logs.tail streamed 4 real gateway lines (WARN+INFO) into the mono stream with per-line level chips, the stat row read "of 4 loaded", and the level chips / search / auto-follow toggle all rendered; zero console errors. Production `vite build` clean and the view IS in the bundle (grep "Live gateway log stream" → index-*.js). Side-by-side pixel diff vs legacy /control/logs deferred to the cutover manual pass |

### overview
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| status RPC {} after waitForConnection → Uptime tile ("Hh Mm Ss" from uptime_ms), version line ("v<version>"), Provider tile | overview.js:232-248 | ported | OverviewPage.tsx statusQuery (queryFn awaits waitForConnection then call('status', {})); OverviewPage.test.tsx::calls status, doctor.status, usage.status and sessions.list after waitForConnection; renders the stat tiles… (asserts '1h 2m 3s', 'v2026.7.19', 'openai'); logic.test.ts::formatUptime. **Live-verified** on :18999 (Uptime "2h 53m 31s", version "v2026.7.19.post1", real status payload) |
| doctor.status RPC {agentId:'main', deep:false} after waitForConnection → Health tile (readiness status label + summary line); on error → "unavailable" / "open health" | overview.js:250-258 | ported | OverviewPage.tsx doctorQuery (deep:false, retry:false) → Health StatTile (readinessStatusLabel + summary); OverviewPage.test.tsx::renders the stat tiles… ('Degraded' + summary); shows the health tile as unavailable when doctor.status rejects. **Live-verified** on :18999 (Health tile "Action required" / "1 action required" from the real shallow report) |
| usage.status RPC {} after waitForConnection → Total tokens tile (toLocaleString), cost line ("$"+toFixed(4)), Total sessions tile | overview.js:260-270 | ported | OverviewPage.tsx usageQuery → Total tokens/Total sessions tiles + cost hint; logic.test.ts::formatTokens / formatCost; OverviewPage.test.tsx::renders the stat tiles… ('1,234,567', '$4.2000 spent', '12'). **Live-verified** on :18999 ("$0.0000 spent" — zero usage on the fresh gateway) |
| sessions.list RPC {limit:5} after waitForConnection → recent sessions list: sort by updated_at desc, slice to 6, per-row dot(status)/key/model/msg-count/relTime; empty state | overview.js:272-310 | ported | OverviewPage.tsx sessionsQuery → sortRecentSessions → ov-recent rows; logic.test.ts::sortRecentSessions (desc sort, missing→0, slice 6, no-mutate); OverviewPage.test.tsx::renders recent sessions sorted newest-first…; shows the empty state when sessions.list returns none. **Live-verified** on :18999 (empty state rendered — no sessions on the fresh gateway) |
| Readiness status label mapping (ready/degraded/action_required/unavailable/unknown else Title-case) | overview.js:352-365 | ported | logic.ts readinessStatusLabel; logic.test.ts::readinessStatusLabel (5 known tokens + Title-case unknown + Unknown fallback) |
| Uptime formatting from uptime_ms → "Hh Mm Ss" (null → "—") | overview.js:234-242 | ported | logic.ts formatUptime; logic.test.ts::formatUptime (1h 2m 3s, 0h 0m 0s, null→—) |
| Session status → dot color class + tooltip label (running/done/failed/killed/timeout) | components.js:249-287 (sessionStatusClass/sessionStatusLabel) | ported (delta) | logic.ts sessionStatusClass/sessionStatusLabel ported 1:1 from components.js; logic.test.ts covers all 5 statuses + fallbacks. **Delta:** the legacy dot color variant (ok/warn/err/off) is mapped to the `--tone` primitive (dotTone: err→danger, off→dim) so the row status dot is a single toned gutter — status color via --tone only, never hardcoded |
| Relative time formatting (just now / Xm/Xh/Xd ago; numeric epoch s/ms handling; invalid → "—") | components.js:228-241 (relTime) | ported | logic.ts relTime ported 1:1; logic.test.ts::relTime (just now / m/h/d ago; epoch seconds ×1000 vs millis passthrough; invalid→—) |
| Wildcard `*` event subscription → prepend to event log (ts HH:MM:SS + name + truncated 80-char JSON payload), cap 30, "N events" counter, is-fresh flash on newest | overview.js:166-169,317-350 | ported | OverviewPage.tsx rpc.on('*') useEffect → events state (newest-first, slice EVENT_LOG_CAP=30); logic.ts formatEventTs/formatEventPayload; logic.test.ts::formatEventPayload (80-char truncate + ellipsis, String() fallback); formatEventTs (HH:MM:SS); OverviewPage.test.tsx::appends wildcard events to the live event stream (newest first, capped counter — "1 event"/"2 events") |
| rpc.state event subscription → refresh connection pill (NOT a card refetch) | overview.js:159-163,192-210 | ported (delta) | Pill derives from the reactive useConnection store (RpcState), so no per-view rpc.state subscription is needed; OverviewPage.test.tsx::reflects the connection state in the overview pill (connecting variant). **Delta:** legacy mirrored the topbar pill via an rpc.state handler + 2s tick; the console's single authoritative connection store replaces both |
| Event handling does NOT invalidate/refetch cards: legacy `*` feeds only the event log and `rpc.state` feeds only the pill; card data refreshes solely via initial load + Refresh + 30s interval | overview.js:159-179,232-311 | ported | Invalidation mapping verified against legacy: the `*` handler calls only setEvents; there is no rpc.on→invalidateQueries anywhere in OverviewPage. OverviewPage.test.tsx::does NOT refetch card RPCs when events arrive (events feed only the log) — asserts the RPC call count is unchanged after emitting `*` + rpc.state |
| Cards refresh on: initial render, Refresh button, and a 30000ms interval | overview.js:142,171-178 | ported | OverviewPage.tsx four useQuery reads with refetchInterval POLL_MS=30000; Refresh → queryClient.invalidateQueries(['overview']); OverviewPage.test.tsx::refetches all four card RPCs when Refresh is clicked. **Live-verified** Refresh reloads the tiles on :18999 |
| Connection pill: mirrors the authoritative connection state (connected/connecting/disconnected → ok/warn/err variant) | overview.js:192-210 | ported (delta) | OverviewPage.tsx PILL_VARIANT (connected→ok/connecting→warn/disconnected→err via --tone); OverviewPage.test.tsx::reflects the connection state in the overview pill. **Delta:** color via the --tone primitive (tone-ok/warn/danger) instead of the legacy conn-pill ok/warn/err classes. **Live-verified** "Connected" on :18999 |
| Stat-tile navigation: Total tokens→/usage, Total sessions→/sessions, Provider→/agents, Health→/health; Uptime tile static | overview.js:50-79,137-139 | ported | OverviewPage.tsx StatTile (button with useNavigate for `to`, static div when omitted); OverviewPage.test.tsx::navigates from stat tiles and the recent row (/sessions, /agents asserted; Uptime tile is a non-button div) |
| Header actions: Refresh (reload cards), Open chat (→/chat); "View all →" + sessions tile → /sessions; recent row → /chat?session=<key> | overview.js:40-46,89,143-144,307-309 | ported | OverviewPage.tsx Refresh/Open chat buttons + ov-link + recent-row onClick=navigate(`/chat?session=${encodeURIComponent(key)}`); OverviewPage.test.tsx::navigates from stat tiles and the recent row (asserts /chat and /chat?session=sess-b); refetches all four card RPCs when Refresh is clicked |
| Connection panel: WebSocket URL + Token inputs prefilled from stored settings; Connect saves settings + reconnects; Disconnect | overview.js:102-116,147-156 | ported | OverviewPage.tsx ov-form (wsUrl seeded from localStorage['agentos.wsUrl']?? bootstrap.ws_url, token from sessionStorage['agentos.wsToken']); onConnect persists (app.js:209-214 parity) then rpc.disconnect()+rpc.connect(url, token||undefined); OverviewPage.test.tsx::renders the connection panel prefilled and reconnects on Connect (asserts localStorage write + connect args). **Live-verified** panel prefilled on :18999 |
| View CSS (ov-* classes) rebuilt on the shared token system | css/views/overview.css | ported (restyled) | views/overview/overview.css on the globals.css token system: header hero + AsciiField backdrop (Health posture), hairline stat-tile grid (Health count-tile posture) with a single --tone gutter on the Health tile, .panel recent/connection/event-stream boxes, toned status dots, mono event log with is-fresh flash; status color only via --tone, lime signal-only (event-log accent/pulse + $ prompt semantics). Verified in-browser (dark theme) on :18999 |
| Live parity: /overview vs legacy /control/overview | overview.js (whole view) | RTL + live-verified | Behaviors covered by 12 OverviewPage RTL + 38 overview-logic unit tests. **Live-verified in-browser** against the real worktree gateway on :18999 (vite dev :5173, /control/overview): connection pill "Connected", all four cards resolved progressively (Uptime "2h 53m 31s" + "v2026.7.19.post1", Provider tile, Health "Action required"/"1 action required" from the shallow doctor.status, usage "$0.0000 spent"), recent-sessions empty state, event stream "Listening for events…" / "0 events", Overview nav item highlighted, doc title "Overview - AgentOS Control"; ZERO console errors. Side-by-side pixel diff vs legacy /control/overview deferred to the cutover manual pass |

### channels
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| channels.status RPC {} after waitForConnection; parallel channels.access.list {} (`.catch(()=>({channels:[]}))` so an access failure never blocks the status render) | channels.js:74-84 | ported | ChannelsPage.tsx channelsQuery (waitForConnection → Promise.all([status, access.list.catch])); ChannelsPage.test.tsx::calls channels.status and channels.access.list after waitForConnection; still renders channels when channels.access.list rejects |
| Merge access-by-name onto channels; drop channels with configured===false | channels.js:85-90 | ported | logic.ts mergeChannels (filter configured!==false, join accessByName); logic.test.ts::mergeChannels (drops configured:false; attaches access by name; null access when unmatched) |
| Sort: pending-access count desc, then status-urgency order (running/connected 0, restarting/exhausted/dead 1, stopped 2, disabled 3; unknown→1) | channels.js:91-103 | ported | logic.ts sortChannels/STATUS_ORDER; logic.test.ts::sortChannels (pending count wins; then urgency order; stable/unknown→1) |
| Stat row: Total (+distinct type count), Connected (+live/idle/unhealthy hint), Inactive (total−connected−attention, +disabled/idle hint), Restart attempts (sum), Chat approvals (sum pending; attention tone when >0) | channels.js:110-151 | ported | logic.ts channelStats/inactiveHint; logic.test.ts::channelStats (counts, restart sum, pending sum, type set); inactiveHint; ChannelsPage.tsx ch-stats; ChannelsPage.test.tsx::renders the stat row from the channel payload |
| needsAttention(status): dead/restarting/exhausted | channels.js:398-400 | ported | logic.ts needsAttention; logic.test.ts::needsAttention (dead/restarting/exhausted true; others false) |
| Channel card: dot+chip variant by status (running/connected→ok, dead→err/danger, else off), name, type chip, status chip, Connected relTime (connected_since), Restart attempts, adapter-config `<details>` JSON | channels.js:202-241 | ported | logic.ts channelDisplay (status resolution, tone mapping via --tone, since/attempts); logic.test.ts::channelDisplay (status fallback connected→connected/else stopped; tone ok/danger/off); ChannelsPage.tsx ChannelCard; ChannelsPage.test.tsx::renders a channel card with status chip, type and config details |
| Status hint footnote: disabled / dead / running / restarting / exhausted / configured-not-active, with `agentos channels restart <name>` command where relevant | channels.js:388-396 | ported | logic.ts statusHint; logic.test.ts::statusHint (all six branches incl. restart command name interpolation); ChannelsPage.tsx card footnote |
| Telegram access panel rendered ONLY for type==='telegram' with access data; mode select (pairing/allowlist/open/disabled, invalid→pairing); locked warning when locked_until*1000 > now; open/disabled notes; pending + approved groups otherwise | channels.js:246-290 | ported | logic.ts resolveAccessMode/isAccessLocked; logic.test.ts::resolveAccessMode (valid pass, invalid→pairing); isAccessLocked (future→true, past/zero→false); ChannelsPage.tsx AccessPanel (gated on type+access); ChannelsPage.test.tsx::renders the telegram access panel only for telegram channels; shows the pending/approved groups |
| senderLabel: @username else display_name else "Telegram user <id>"; senderMeta: display_name (if distinct) · ID <id> · expires <localeTime> · source | channels.js:373-386 | ported | logic.ts senderLabel/senderMeta; logic.test.ts::senderLabel (username/display_name/fallback); senderMeta (bits joined with ·; drops duplicate display_name) |
| Mode select change → channels.access.setMode {channel, mode}; success toast (warn when 'open', else info) + refetch; failure toast + refetch | channels.js:319-334 | ported | ChannelsPage.tsx onSetMode mutation → invalidateQueries(['channels']); ChannelsPage.test.tsx::changing the access mode calls channels.access.setMode and invalidates; open mode warns |
| Pending Approve/Deny → channels.access.resolve {channel, senderId, approved}; success toast + refetch; failure toast (button re-enabled) | channels.js:336-353 | ported | ChannelsPage.tsx onResolve mutation → invalidateQueries; ChannelsPage.test.tsx::approving a pending account calls channels.access.resolve with approved:true and invalidates; denying calls approved:false |
| Approved Revoke → channels.access.revoke {channel, senderId}; success toast + refetch; failure toast (button re-enabled) | channels.js:355-370 | ported | ChannelsPage.tsx onRevoke mutation → invalidateQueries; ChannelsPage.test.tsx::revoking an approved account calls channels.access.revoke and invalidates |
| Real-time `channel.status` event subscription → invalidate/refetch (targeted, cleaned up on unmount) | channels.js:55-56,64-72 | ported | ChannelsPage.tsx useEffect rpc.on('channel.status', …) → invalidateQueries(['channels']) with unsub cleanup; ChannelsPage.test.tsx::refetches when a channel.status event arrives; unsubscribes from channel.status on unmount. **Delta:** legacy also ran a 5000ms setInterval reload (channels.js:60-61); the console uses react-query refetchInterval POLL_MS=5000 for the same cadence, and the event drives a targeted invalidate rather than a full imperative _loadData |
| 5000ms poll cadence for channels.status/access.list | channels.js:60-61 | ported (delta) | ChannelsPage.tsx channelsQuery refetchInterval POLL_MS=5000. **Delta:** legacy setInterval(_loadData,5000) → react-query refetchInterval (same 5s cadence; single source of truth with the event-driven invalidate) |
| Refresh button reloads channels | channels.js:36-39,52 | ported | ChannelsPage.tsx Refresh → queryClient.invalidateQueries(['channels']); ChannelsPage.test.tsx::refreshes on the Refresh button |
| Empty state: art + copy, Guided-setup button → navigate('/setup'), two CLI command readouts | channels.js:163-199 | ported (delta) | ChannelsPage.tsx ch-empty (Guided setup Button → useNavigate('/setup'), two <CommandLine> readouts); ChannelsPage.test.tsx::shows the empty state with a guided-setup link into /setup when no channels are configured. **Delta:** legacy `agentos onboard configure channels` / `agentos channels list` code chips rebuilt as common <CommandLine> readouts (copy affordance); guided-setup uses react-router navigate instead of legacy Router.navigate |
| Load failure toast ("Failed to load channels: …") | channels.js:107 | ported (delta) | ChannelsPage.tsx channelsQuery onError-style toast in the queryFn catch is not used; instead the error surfaces via query.isError → a toast in an effect + inline error note; ChannelsPage.test.tsx::toasts when channels.status fails. **Delta:** legacy caught in _loadData and toasted; the port keeps the same message via a react-query error branch |
| Toast contract (sonner seam) | components.js UI.toast | ported (delta) | All channels toasts use sonner (toast.success/warning/error) with stable per-action ids; the assertive-announce seam limitation recorded on the health copy-toast row applies |
| View CSS (ch-* classes) rebuilt on the shared token system | css/views/channels.css | ported (restyled) | views/channels/channels.css on the globals.css token system: header hero + AsciiField backdrop (Health posture), hairline stat tiles with a --tone hero/attention gutter, .panel channel cards with a status --tone gutter (never hardcoded), telegram access sub-panel (mode select + pending/approved people rows), dashed empty state with guided-setup; status color only via --tone, lime signal-only |
| Live parity: /channels vs legacy /control/channels | channels.js (whole view) | RTL + live-verified (empty state) | Behaviors covered by 15 ChannelsPage RTL + 36 channels-logic unit tests. **Live-verified in-browser** against the real worktree gateway on :18999 (vite dev :5173, /control/channels): connection pill "Connected", stat row rendered (Total channels "0 types" / Connected "all idle" / Inactive "no inactive channels" / Restart attempts / Chat approvals "no accounts waiting"), the empty state rendered (fresh gateway has zero configured channels) with the Guided-setup button + two <CommandLine> readouts (`agentos onboard configure channels`, `agentos channels list`), Channels nav item present, doc title "Channels - AgentOS Control"; ZERO console errors. Channel-card + telegram access-panel visuals (card tone gutter, mode select, pending/approved rows, approve/deny/revoke) can't be live-driven with no configured Telegram channel at check time — covered by the ChannelsPage RTL (setMode/resolve/revoke RPC + invalidation, channel.status event → targeted refetch, unmount cleanup). Side-by-side pixel diff + a configured-channel card deferred to the cutover manual pass |

### agents
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| agents.list {} after waitForConnection → {agents} | agents.js:84-91 | ported | AgentsPage.tsx agentsQuery (waitForConnection → agents.list); AgentsPage.test.tsx::calls agents.list after waitForConnection |
| isBuiltin resolution: type==='builtin' OR isBuiltin flag | agents.js:97,144-146 | ported | logic.ts isBuiltinAgent; logic.test.ts::isBuiltinAgent (type/flag true; custom/absent false) |
| Stat row: Total agents (+built-in/custom split hint), Models in use (distinct non-empty models), Tools wired (sum of tools lengths) | agents.js:93-118 | ported | logic.ts agentStats; logic.test.ts::agentStats (totals, builtin/custom split, distinct models, tool sum, empty list, non-array tools); AgentsPage.tsx ag-stats; AgentsPage.test.tsx::renders the stat row from the agents payload |
| Agent card: id, dot+type chip (builtin→ok / custom→info tone), name (fallback id↔name), description, model, tool/skill counts, first-8 tool chips + overflow (+N) | agents.js:141-179 | ported | logic.ts agentDisplay (id/name/type/tone/counts/chips/overflow; status color ONLY via --tone); logic.test.ts::agentDisplay (tone ok/info, chip limit + overflow, type derivation, em-dash fallback); AgentsPage.tsx AgentCard; AgentsPage.test.tsx::renders a card per agent with type chip and tool chips |
| Card actions: builtin → Chat + Customize (no Delete); custom → Chat + Edit + Delete | agents.js:151-165,196-219 | ported | AgentsPage.tsx AgentCard action row; AgentsPage.test.tsx::builtin cards expose Customize (no Delete); custom cards expose Edit + Delete |
| Chat button → navigate('/chat?agent=<id>') (encoded) | agents.js:196-201 | ported (delta) | AgentsPage.tsx onChat → useNavigate('/chat?agent='+encodeURIComponent(id)); AgentsPage.test.tsx::Chat button navigates to /chat with the agent query. **Delta:** legacy Router.navigate → react-router useNavigate |
| Create: id required (trimmed non-empty) → agents.create {id, name?} (name only when provided); success toast + reload; agent.exists → warn toast (not error) | agents.js:224-240,226-230 | ported (delta) | logic.ts validateAgentId/validateCreate/buildCreatePayload; logic.test.ts::validateAgentId; validateCreate; buildCreatePayload (trim, omit blank name); AgentsPage.tsx createMutation → invalidateQueries(['agents']); AgentsPage.test.tsx::opening New agent … calls agents.create and invalidates; blocks create submit … when the id is blank; warns (not errors) when agents.create reports agent.exists. **Delta:** legacy inline-form → shadcn-posture Dialog (native tokenized modal); validation surfaced inline in logic.ts and blocks submit before the RPC |
| Customize from builtin: seed create form with `<id>-copy` id + `<id> (copy)` name | agents.js:242-256 | ported (delta) | AgentsPage.tsx openCustomize seeds the create dialog; AgentsPage.test.tsx::Customize on a builtin opens the create dialog pre-seeded with <id>-copy. **Delta:** legacy pre-filled the inline form + scrollIntoView; the port opens the create Dialog pre-seeded (same `<id>-copy` seed) |
| Edit dialog: Identity (id disabled post-create, name, description) + Advanced (tools csv, workspace, agentDir, enabled) seeded from the agent | agents.js:258-369,322-362 | ported (delta) | logic.ts agentToForm (enabled default true, agentDir camel>snake, tools copy) + parseToolsInput; logic.test.ts::agentToForm; parseToolsInput; AgentsPage.tsx AgentDialog (id disabled in edit); AgentsPage.test.tsx::editing … id field is disabled. **Delta:** legacy view/edit *drawer* → a single edit **Dialog** (Identity + Advanced fields identical); the drawer's read-only "view" mode is folded into the card front (id/type/name/desc/model/tool+skill counts/tool chips) + a direct Edit button. The dirty-guard discard-confirm and no-op-save are both restored (separate rows below) |
| Update: diff seed vs edited → agents.update {id, …changed}; **no-op save (only {id}) skips the RPC → 'Nothing to save' toast, dialog stays open**; success toast + reload; not_found / builtin_immutable friendly messages | agents.js:426-476,432-437,467-476 | ported | logic.ts buildUpdatePayload + isNoOpUpdate; logic.test.ts::buildUpdatePayload (only-changed scalars, tools structural diff, no-change→{id}); isNoOpUpdate (only-id→true, any change→false); AgentsPage.tsx onSave short-circuits isNoOpUpdate → toast.info('Nothing to save') without mutating, else updateMutation → invalidateQueries; AgentsPage.test.tsx::editing a custom agent sends only changed fields to agents.update and invalidates; saving an unchanged edit does not call agents.update and toasts "Nothing to save"; surfaces a friendly message when agents.update hits builtin_immutable |
| Dirty-guard on close: a dirty edit (current form ≠ seed) prompts "Discard unsaved changes?" ("You have unsaved edits. Closing now will lose them.") on Escape / backdrop / Cancel — close only on confirm; create mode / non-dirty edit close immediately | agents.js:272-275,307-312,499-506 | ported | logic.ts isFormDirty (structural JSON compare, tools folded in); logic.test.ts::isFormDirty (unchanged false, scalar/tools change true); AgentsPage.tsx AgentDialog attemptClose → dirty ? ConfirmDialog(discard) : onCancel, wired to Escape/backdrop/Cancel; AgentsPage.test.tsx::closing a dirty edit via Escape shows the discard confirm; dismissing keeps edits; confirming discard on a dirty edit closes the dialog; closing a non-dirty edit via Escape closes immediately with no discard prompt |
| Delete: confirmation (destructive) → agents.delete {id}; success toast + reload; failure toast | agents.js:508-520 | ported (delta) | AgentsPage.tsx ConfirmDialog (role="alertdialog", Button variant="destructive") → deleteMutation → invalidateQueries; AgentsPage.test.tsx::deleting requires confirmation then calls agents.delete and invalidates; cancelling the delete confirmation does not call agents.delete. **Delta:** legacy UI.modal MutationObserver confirm → tokenized alertdialog modal + destructive button |
| Refresh button reloads agents | agents.js:37-40,68 | ported | AgentsPage.tsx Refresh → queryClient.invalidateQueries(['agents']); AgentsPage.test.tsx::refreshes on the Refresh button |
| Empty state: "No agents configured." + copy referencing the default `main` agent | agents.js:132-139 | ported | AgentsPage.tsx ag-empty; AgentsPage.test.tsx::shows the empty state when no agents are configured |
| Load-failure toast ("Failed to load agents: …") | agents.js:90 | ported (delta) | AgentsPage.tsx agentsQuery.isError → toast effect (stable id); AgentsPage.test.tsx::toasts when agents.list fails. **Delta:** legacy caught in _loadData; the port surfaces via a react-query error branch (same message) |
| Document title "Agents - AgentOS Control" | js/router.js meta.title | ported | AgentsPage.tsx useEffect document.title; AgentsPage.test.tsx::sets the document title |
| Toast contract (sonner seam) | components.js UI.toast | ported (delta) | All agents toasts use sonner (toast.success/warning/error) with stable per-action ids; the assertive-announce seam limitation recorded on the health copy-toast row applies |
| Whole-card click / Enter-Space → open the read-only view drawer; `_esc` HTML escaping | agents.js:182-194,522-528 | waived (design) | Only the whole-card-click/keyboard affordance that opened the legacy read-only *view drawer* is dropped: the card front now shows the same readout inline (id/type/name/desc/model/counts/tool chips) with explicit Chat / Edit / Customize / Delete buttons, so there is no separate view surface to open (brief: create/edit go through a Dialog). `_esc` is obsolete — React escapes text nodes. NOTE: the dirty-guard discard-confirm and no-op-save that were previously folded into this waiver are now RESTORED to legacy behavior (see the Update + Dirty-guard rows above) — this waiver no longer covers them |
| View CSS (ag-* classes) rebuilt on the shared token system | css/views/agents.css | ported (restyled) | views/agents/agents.css on the globals.css token system: header hero + AsciiField backdrop (Health posture), hairline stat tiles with a lime hero gutter, .panel agent cards with a builtin-ok / custom-info --tone gutter+dot+type chip (never hardcoded), tool chips, tokenized create/edit Dialog + destructive delete alertdialog (overlay + focus + Escape/backdrop close), dashed empty state; status color only via --tone, lime signal-only |
| Live parity: /agents vs legacy /control/agents | agents.js (whole view) | RTL (full) + live smoke (partial) | Behaviors covered by **21 AgentsPage RTL + 29 agents-logic unit tests** (the authoritative coverage). A **partial live smoke** ran in-browser against the worktree gateway on :18999 (vite dev :5173, /control/agents) and drove only two surfaces: (1) the initial render with the single builtin `main` agent — connection pill "Connected", header hero (Control · Agents), stat row (Total agents "1 built-in" / Models in use "unset" / Tools wired "0"), the `main` card (ok-tone gutter+dot, `builtin` chip, "Main Agent" / "Primary AgentOS agent", Chat + Customize, NO Delete), Agents nav highlighted, doc title "Agents - AgentOS Control"; and (2) opening the empty New-agent Dialog (dimmed backdrop, tokenized panel, Agent ID focused with lime ring, Create-agent CTA) — then closed. ZERO console errors on both. **NOT driven live:** the create/edit/delete RPCs, form validation, the dirty-guard discard-confirm, no-op-save, `agent.exists`/`builtin_immutable` paths, and any custom-agent card (only the builtin `main` existed on the fresh gateway). Those are covered by RTL only. A full custom-agent CRUD pass + side-by-side pixel diff against legacy is deferred to the cutover manual pass |

### sessions
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| Initial load: parallel sessions.list {limit:200} + agents.list after waitForConnection; agents map keyed by id (orphan detection); sessions failure → error toast, agents failure keeps last map | sessions.js:126-160 | ported (delta) | SessionsPage.tsx sessionsQuery (waitForConnection → sessions.list {limit:200}) + agentsQuery (agents.list {}); SessionsPage.test.tsx::loads sessions {limit:200} and agents.list after waitForConnection. **Delta:** legacy Promise.allSettled in one _loadData → two react-query useQuery reads (still parallel); a failed agents.list keeps the last map via react-query retaining prior `data` (legacy's "don't clear the map" invariant), and orphan chips gate on agentsQuery.isSuccess (legacy `_agentsLoaded`) |
| Stat row: Total sessions (+open/completed/failed-timedout/aborted hint), Executing (queued+running run-status, active dot), Messages (localized sum + distinct-agent count hint) | sessions.js:196-241 | ported | logic.ts sessionStats (total/lifecycleOpen/activeRuns/done/failedOrTimedOut/aborted/totalMessages/agents); logic.test.ts::sessionStats (buckets, run folding, distinct agents, empty); SessionsPage.tsx sess-stats + StatTile; SessionsPage.test.tsx::renders the stat row from the sessions payload. Storage KPI intentionally omitted (legacy note sessions.js:238-240 — backend always None) |
| Search: matches key/model/display_name/subject/derived_title (case-insensitive), debounced 180ms, resets page + selection | sessions.js:84-97,162-176 | ported | logic.ts filterSessions (5-field match, copy on empty); logic.test.ts::filterSessions (key/model/display_name/subject/derived_title); SessionsPage.tsx 180ms debounce useEffect → setPage(0)+clear selection; SessionsPage.test.tsx::filters the table via the search box (matches display_name) |
| Sort: key/updated_at/message_count columns; numeric vs string compare; toggle asc/desc; default updated_at desc | sessions.js:180-194,282,395-408 | ported | logic.ts sortSessions (numeric for updated_at/message_count, lowercased string else, asc flag, no-mutate); logic.test.ts::sortSessions (numeric/string/no-mutate); SessionsPage.tsx onSort toggles + sortArrow/ariaSort; default sortCol='updated_at' sortAsc=false. Covered indirectly by the render + filter RTL tests |
| Pagination: page-size 10/25/50/100 (default 25), prev/next, clamp page to range, "N / M · T total" info | sessions.js:98-103,248-250,456-467 | ported | SessionsPage.tsx PAGE_SIZES select (default 25) + prev/next Buttons + safePage clamp (Math.min(page, totalPages-1)) + "N / M · total" info; slice = filtered.slice(page*size,…). Structural port; exercised via the render tests (default page shows all 3 rows) |
| Session row: status dot (tone) + tooltip, key link → chat, agent subline / orphan chip, status chip + run-status badge, msg count, relTime, copy + delete actions | sessions.js:302-333 | ported (delta) | SessionsPage.tsx table row: sess-dot tone-* + title, sess-key-link, agentSubline, sess-chip (status) + runStatusBadge chip, message_count, relTimeLabel, copy + delete icon buttons; SessionsPage.test.tsx::renders a row per session with key link and status. **Delta:** legacy innerHTML + `_esc` → React nodes (auto-escaped); status color on the --tone primitive (never the legacy hardcoded dot/chip color classes) |
| Visual status derivation: run-status (active/last/terminal task) folds failed/timeout/killed over session.status; run-status normalization (abandoned→interrupted, succeeded→idle, …) | sessions.js:726-790 | ported | logic.ts normalizeRunStatus/terminalRunStatus/sessionRunStatus/sessionVisualStatus/runStatusBadge ported 1:1; logic.test.ts covers each (active-wins, terminal fold, normalization map, badge label+tone, idle→null) |
| Orphan-agent subline: agent id from row or key prefix; known agent → name; unknown after agents loaded → ⚠ Orphaned chip; `main` suppressed; pre-load shows plain id | sessions.js:796-824,720-724 | ported (delta) | logic.ts agentIdFromKey + agentSubline (returns {name,orphan} descriptor, not HTML); logic.test.ts::agentIdFromKey; agentSubline (blank/main→null, known→name, unknown+loaded→orphan, unknown pre-load→plain); SessionsPage.tsx renders the descriptor → sess-key__agent(+--orphan) + tone-warn ⚠ chip; SessionsPage.test.tsx::marks an orphaned agent once the registry loads. **Delta:** helper returns data, the component renders the chip |
| Jump-to-chat: key link + chat icon → navigate('/chat?session=<key>') (encoded); chat is Plan-3 StubView | sessions.js:363-370,327 | ported (delta) | SessionsPage.tsx key-link + chat-icon onClick → useNavigate('/chat?session='+encodeURIComponent(key)); SessionsPage.test.tsx::the key link navigates to /chat?session=<key> (encoded). **Delta:** legacy Router.navigate → react-router useNavigate; /chat is still the StubView (chat migration is Plan 3), so this is a link-only target as specified |
| Copy session key → clipboard + ok/warn toast | sessions.js:373-384 | ported (delta) | SessionsPage.tsx copyKey → navigator.clipboard.writeText + toast.success('Copied session key')/toast.warning('Copy failed'); SessionsPage.test.tsx::copies a session key to the clipboard and toasts. **Delta:** the legacy execCommand textarea fallback is not reproduced inline here (kept simple, matching the primary clipboard path); the shared CommandLine fallback pattern remains available if needed. Toast via sonner seam |
| Delete one: confirm modal (destructive, /reset backup note) → sessions.delete {key}; parse {deleted,errors}; success/failure toast; reload | sessions.js:529-559 | ported (delta) | logic.ts buildDeleteParams (single→{key}) + parseSingleDeleteResult; logic.test.ts::buildDeleteParams; parseSingleDeleteResult (ok / reason variants); SessionsPage.tsx ConfirmDialog(alertdialog, destructive, /reset note) → deleteMutation([key]) → invalidateQueries(['sessions']); SessionsPage.test.tsx::deleting one session confirms then calls sessions.delete {key} and reloads; cancelling the delete confirmation does not call sessions.delete. **Delta:** legacy UI.modal → tokenized alertdialog |
| Bulk-select: per-row + select-all-on-page checkboxes, bulk bar (count, Clear, Delete selected), selection cleared on load/search/page | sessions.js:284-360,469-492 | ported | SessionsPage.tsx selected Set + toggleRow/toggleAllOnPage + sess-bulk-bar (count/Clear/Delete selected, shown only when selection non-empty); selection cleared by the search-debounce effect + on delete success; SessionsPage.test.tsx::bulk-selecting rows and deleting calls sessions.delete {keys:[…]} (exercises row checkboxes + bulk bar) |
| Bulk delete: confirm modal (N sessions) → sessions.delete {keys:[…]}; {deleted,errors} partial-failure toast; reload | sessions.js:494-527 | ported | logic.ts buildDeleteParams (multi→{keys}) + parseBulkDeleteResult; logic.test.ts::buildDeleteParams; parseBulkDeleteResult (deleted/errors + fallback count); SessionsPage.tsx bulk ConfirmDialog → deleteMutation(keys) → partial-failure toast.warning / success toast + invalidate; SessionsPage.test.tsx::bulk-selecting rows and deleting calls sessions.delete {keys:[…]} |
| Create session: modal with agent picker (agents.list, `main` default, allow-create typed id) → optional agents.create {id,name} (agent.exists tolerated) then sessions.create {agentId}; navigate to new session's chat; friendly inline errors (UNAUTHORIZED/not_found/exists); toast | sessions.js:561-716 | ported (delta) | SessionsPage.tsx NewSessionDialog (agent input + datalist, `main` default when present, exact-match→select / novel id→create) + createMutation (agents.create then sessions.create, agent.exists tolerated) → navigate('/chat?session='+encode(key)); inline createError kept open on failure with friendly UNAUTHORIZED/not_found/exists messages; SessionsPage.test.tsx::creating a session with an existing agent calls sessions.create and navigates; creating a session with a new agent id creates the agent first, then the session; surfaces an inline error and keeps the create dialog open when sessions.create fails. **Delta:** legacy UI.combobox (custom allow-create widget) → a text input backed by a native `<datalist>` with the same select-or-create semantics (exact id/label match selects; any other typed id is a pending create). The legacy live "Creating…/Starting…" button relabel is preserved |
| Empty states: no sessions ("No sessions yet." + "Start a new session" CTA) vs no search matches ("No matches") | sessions.js:259-267,411-449 | ported (delta) | SessionsPage.tsx sess-empty (no sessions, CTA opens the new-session dialog) vs sess-empty--search ("No matches"); SessionsPage.test.tsx::shows the empty state with a create CTA when there are no sessions; shows the "No matches" empty state when a search excludes everything. **Delta:** the legacy decorative inline SVG art is dropped (token-system empty state, matching the agents-view posture); copy + CTA preserved |
| Refresh button reloads sessions | sessions.js:79,126 | ported (delta) | SessionsPage.tsx Refresh → queryClient.invalidateQueries(['sessions']); SessionsPage.test.tsx::refreshes sessions on the Refresh button. **Delta:** legacy _loadData refreshed sessions AND agents; the port invalidates the `['sessions']` key (both the sessions read and the `['sessions','agents']` read are nested under it, so agents refresh too) |
| Document title "Sessions - AgentOS Control" | js/router.js meta.title | ported | SessionsPage.tsx useEffect document.title; SessionsPage.test.tsx::sets the document title |
| Toast contract (sonner seam) | components.js UI.toast | ported (delta) | All sessions toasts use sonner (success/warning/error) with stable per-action ids; the assertive-announce seam limitation recorded on the health copy-toast row applies |
| Load-failure toast ("Failed to load sessions: …") | sessions.js:158 | ported (delta) | SessionsPage.tsx sessionsQuery.isError → toast effect (stable id); SessionsPage.test.tsx::toasts when sessions.list fails. **Delta:** legacy caught in _loadData; the port surfaces via a react-query error branch (same message) |
| View CSS (sess-* classes) rebuilt on the shared token system | css/views/sessions.css | ported (restyled) | views/sessions/sessions.css on the globals.css tokens: header hero + AsciiField backdrop, hairline stat tiles with a lime hero gutter, bulk bar with a --tone rail, hairline table (sortable headers, tone status dot+chips, orphan warn chip), tokenized new-session Dialog + destructive delete/bulk alertdialogs (overlay + focus + Escape/backdrop close), dashed empty states; status color only via --tone, lime signal-only |
| Search debounce / sort / pagination are UI-local (no localStorage; no polling) | sessions.js:83-103,180-194 | ported | Legacy kept these in module-local vars with no persistence and no interval poll (the view had no `_intervals` in use); the port keeps them in component state (search debounce, sortCol/sortAsc, page/pageSize). No localStorage keys, no polling cadence to port |
| Live parity: /sessions vs legacy /control/sessions | sessions.js (whole view) | RTL (full) + live pending | Behaviors covered by **18 SessionsPage RTL + 41 sessions-logic unit tests** (the authoritative coverage) + a clean production `npm run build` (the view compiles into the real bundle). **Live smoke NOT run:** the React dev server (vite :5173) was DOWN and the brief said reuse-don't-restart; the gateway on :18999 serves the LEGACY static console (base.css/components.css/static/js), not the built React app, so it cannot exercise the new SessionsPage. A live in-browser pass on :5173 + side-by-side vs legacy /control/sessions is deferred to the cutover manual pass |

### usage
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| Initial load + polling: usage.status {} after waitForConnection → sessions[]; render all sections; 60s setInterval; skip fetch while tab hidden; resume on visibilitychange; failure → error toast | usage.js:145-157,350-366 | ported (delta) | UsagePage.tsx usageQuery (waitForConnection → usage.status, sessions ?? []) with `refetchInterval:60_000` + `refetchIntervalInBackground:false` + `refetchOnWindowFocus:true`; error branch → toast.error (stable id); UsagePage.test.tsx::calls usage.status after waitForConnection; toasts when usage.status fails. **Delta:** legacy setInterval + a manual visibilitychange handler that early-returns while hidden → react-query's interval (which already pauses in the background) + refetchOnWindowFocus reproduces the same 60s cadence + instant-resume-on-focus without hand-rolled visibility wiring; the legacy usage.cost double-poll was already dropped in the legacy file (only usage.status is called) |
| Range filter: All / 7d / 14d / 30d buttons, persisted to localStorage `agentos-usage-range` (default 7, validated to {all,7,14,30}); cutoff = now − Nd; a row is visible if its timestamp (endedAt/updatedAt/startedAt/createdAt, snake or camel) ≥ cutoff; `all` keeps everything | usage.js:14,131-138,174-236 | ported | logic.ts normalizeRange + rangeCutoffMs + sessionTimestamp + visibleSessions; logic.test.ts::normalizeRange (accepts all/7/14/30, defaults invalid/null/non-string to 7); rangeCutoffMs (all→null, 7→now−7d); sessionTimestamp (ended/updated/started/created precedence, snake+camel, null); visibleSessions (all keeps undated, 7d drops undated + out-of-window); UsagePage.tsx range state seeded from localStorage + pickRange persists; UsagePage.test.tsx::persists the range selection to localStorage and re-filters; reads the persisted range on mount |
| Undated-legacy hint: when range≠all, count rows with no timestamp; "N undated legacy session(s) hidden" shown in toolbar + sessions-meta | usage.js:238-264,441-444 | ported | logic.ts undatedHiddenCount + rangeHiddenHint; logic.test.ts::undatedHiddenCount (0 on all, counts undated); rangeHiddenHint (singular/plural, '' on all); UsagePage.tsx usage-range-notice (toolbar) + sessionMeta (sessions section); UsagePage.test.tsx::surfaces the undated-legacy hint when a dated range hides undated rows |
| Metrics tiles: Total tokens (in+out, toLocaleString), Total cost ($ toFixed(4)), Sessions (count), Avg cost/session (cost/count, toFixed(4)); tokens breakdown (In/Out/Cache R/Cache W); cost hint = source composition (actual/estimated/mixed/unpriced/ephemeral counts) over visible rows | usage.js:243-252,312-322,385-423 | ported | logic.ts usageMetrics + sourceCompositionHint; logic.test.ts::usageMetrics (snake/camel sums, totalTokens, avgCost, null on empty); sourceCompositionHint (lowercased non-zero counts, '' empty); UsagePage.tsx usage-stats tiles (.t-data numerals) + breakdown row + cost hint; UsagePage.test.tsx::renders the metric tiles from the payload ('1,200', '$5.5000', '2', '$2.7500') |
| Chart: segmented Tokens\|Cost mode; filters zero-token rows, sorts by total tokens (tokens) or cost (cost) desc, top 20; CSS bars (input+output stacked in tokens mode, single fill in cost mode) scaled to max (max 0→1); value label = _fmtNum (K/M) or _fmtCost; Output legend hidden in cost mode; bar click → /chat?session=<key>; empty → "No data in the selected window." | usage.js:515-627 | ported (restyled) | logic.ts chartRows (zero-token filter, mode sort, top-20, max-scaled input/output %, value label, poolSize/shown) + formatNum; logic.test.ts::chartRows (drops zero rows, sorts desc, %scaling, cost single-fill, top-20 cap + pool size, label truncation, empty→max 1); formatNum (M/K/plain); UsagePage.tsx usage-bars CSS bars (input/output fills, output legend hidden in cost mode) + bar click → navigate; UsagePage.test.tsx::renders chart bars and a bar click navigates to chat; switches the chart to cost mode; shows the empty…states. **Restyled:** legacy DOM-built bars → simple CSS bars on the token system (no chart library, per brief) |
| Chart caption: "Top sessions by total tokens"/"…by cost" + "· showing X of Y" when the non-zero pool exceeds 20 | usage.js:523-538 | ported | logic.ts chartRows returns poolSize + shown; UsagePage.tsx chartCaption composes the "showing X of Y" suffix when poolSize>shown; UsagePage.test.tsx::switches the chart to cost mode (asserts "Top sessions by cost") |
| Sessions table: 9 columns (Session, Modified, Input, Output, Cache R, Cache W, Cost, Source, Model); sortable on session/updated_at/input_tokens/output_tokens/cost_usd/model (string vs numeric compare, toggle asc/desc, default updated_at desc); Session link → /chat; Modified = relTime; numbers .t-data mono; cost = $toFixed(4); cost-source badge; model cell; empty → "No usage data yet" | usage.js:16-26,324-343,425-513 | ported (delta) | logic.ts sortSessions (session/updated_at/input/output/cache/cost sort keys, string-lowercase vs numeric, no-mutate) + formatRelTime; logic.test.ts::sortSessions (numeric/string/timestamp/no-mutate); formatRelTime (sec/ms epoch, buckets, invalid→dash); UsagePage.tsx TABLE_COLUMNS + usage-table (sortable headers with aria-sort, default sortCol='updated_at' sortAsc=false, .t-data mono cells, $toFixed(4) cost, source badge, model cell, empty state); UsagePage.test.tsx::renders a table row per visible session with cost + source badge; a table session link navigates to /chat…(encoded); sorts the table when a sortable header is clicked (new column→desc, toggle→asc); shows the empty…states. **Delta:** legacy innerHTML + `_esc` → React nodes (auto-escaped); the legacy session link is a `<button>` (keyboard-focusable) like the sessions view, not an `<a href="#">` |
| Cost-source badge: label (Actual/Estimated/Mixed/Unpriced/None/Ephemeral) + tooltip; prorated → "Actual" with distinct style + nuance tooltip; ephemeral wins | usage.js:266-310 | ported | logic.ts costSource + costSourceBadge (label/tooltip/cls/ephemeral); logic.test.ts::costSourceBadge (known→label/cls, unknown→none, ephemeral overrides, camelCase costSource); UsagePage.tsx SourceBadge (usage-source--<cls>, dashed border for prorated, ephemeral modifier); UsagePage.test.tsx::renders a table row…with…source badge (Actual + Estimated present). Badge color via source-scoped `--tone` (never lime) |
| Model cell + expand: label (single model, or "auto · N models"); a >1-model breakdown shows an expand toggle → inline breakdown (per-model provider/name, cost share bar+%, tokens, cost, source badge; prorated notice; total line) | usage.js:629-752 | ported (delta) | logic.ts modelDisplayLabel + hasModelExpand + sessionExpandRows (totals, anyProrated, provider/name split, cost share); logic.test.ts::modelDisplayLabel (single/multi/none); sessionExpandRows (>1 build, share, prorated flag); UsagePage.tsx ExpandableRow toggle + ModelExpansion (usage-expand list, prorated note, total line); UsagePage.test.tsx::expands a multi-model session row to show the per-model breakdown. **Delta:** legacy imperative DOM-append expand row → React state (`expanded` Set keyed by session) + conditional expansion `<tr>` |
| By-model breakdown grid: group visible rows by per-model usage (modelBreakdown when present, else the row's single model), summing tokens/cache/cost + distinct-session count; sort by cost desc; per-card cost-share % + bar; empty → "No model usage yet." | usage.js:754-831 | ported | logic.ts modelBreakdownGrid (per-model group + distinct-session count, cost-desc sort, provider/name split, cost share); logic.test.ts::modelBreakdownGrid (grouping, distinct sessions, cost sort, share, empty); UsagePage.tsx usage-model-grid cards (share bar + KV rows); UsagePage.test.tsx::renders the by-model breakdown grid (gpt-4 across sessions + claude) |
| CSV export: headers (session,input,output,cache R/W,cost,billed,estimated,source,missing entries,ephemeral,model) over visible rows; cost fields toFixed(6); quote-escape; filename `agentos-usage-<range>.csv`; download via Blob | usage.js:833-876 | ported | logic.ts buildCsv (12-column header, 6-decimal cost/billed/estimated, quote-escape) + csvFilename (all / `<N>d`); logic.test.ts::buildCsv (header row, quote-escaped embedded quote, 1.500000); csvFilename (all/7d); UsagePage.tsx exportCsv → Blob download over the visible rows; Export button wired. Blob download itself is a thin DOM shim (not unit-tested; identical to the legacy `_download`) |
| Refresh button reloads usage.status | usage.js:47-49,115 | ported (delta) | UsagePage.tsx Refresh → queryClient.invalidateQueries(['usage']); UsagePage.test.tsx::refreshes usage on the Refresh button (usage.status call count 1→2). **Delta:** legacy `_loadData` → react-query invalidate |
| Document title "Usage - AgentOS Control" | js/router.js meta.title | ported | UsagePage.tsx useEffect document.title; UsagePage.test.tsx::sets the document title |
| No CLI command in the legacy view (CommandLine N/A) | usage.js (whole view) | ported (n/a) | The legacy usage view renders no `$ …` command line (grep for cmdline/CommandLine/command in usage.js → none), so the shared `<CommandLine>` requirement does not apply to this view. No deviation — the control simply does not exist in the legacy source |
| View CSS (usage-* classes) rebuilt on the shared token system | css/views/usage.css | ported (restyled) | views/usage/usage.css on the globals.css tokens: header hero + AsciiField backdrop, hairline metric tiles with a lime hero gutter, segmented/range pill toggles, CSS bar chart (lime input + info output fills), by-model cards with cost-share bars, hairline sortable table, source-tinted badges (dashed for prorated), inline model-breakdown expansion; status/source color via `--tone`/explicit source tints, lime signal-only |
| Live parity: /usage vs legacy /control/usage | usage.js (whole view) | RTL (full) + live pending | Behaviors covered by **16 UsagePage RTL + 47 usage-logic unit tests** (the authoritative coverage) + a clean production `npm run build` (the view compiles into the real bundle). **Live smoke NOT run:** the React dev server (vite :5173) was DOWN and the gateway on :18999 serves the LEGACY static console, not the built React app, so it cannot exercise the new UsagePage. A live in-browser pass on :5173 + side-by-side vs legacy /control/usage is deferred to the cutover manual pass |

### config
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| Initial load: config.get after waitForConnection → config tree; failure → error toast | config.js:290-306 | ported (delta) | ConfigPage.tsx configQuery (waitForConnection → config.get, data ?? {}); load-failure branch → toast.error('Failed to load config: …', stable id); ConfigPage.test.tsx::calls config.get after waitForConnection…; toasts when config.get fails. Live-verified against the :18999 gateway on /control/config (config.get resolved the real tree). **Delta:** legacy `_loadData` promise → a react-query useQuery read (same order) |
| Form/YAML mode toggle: Form renders tabbed fields; YAML renders the serialised baseline in a textarea; the sticky-bar count reflects the active mode | config.js:163-193,310-324 | ported | ConfigPage.tsx mode state + `.cfg-mode-toggle` (Form/YAML, aria-pressed); Form → tabs+fields, YAML → `.cfg-yaml__area` seeded from `objToYaml(config)`; count = yaml? (yamlDirty?1:0) : dirtyCount. ConfigPage.test.tsx::YAML mode Save calls config.apply…(switches mode, reads the baseline). Live-verified: Form↔YAML toggle drove both surfaces, YAML showed the full serialised tree |
| Six form tabs (Core / AI & Agents / Memory / Communication / Automation / Infrastructure), each collecting top-level keys by prefix (exact / prefix+dot / prefix+underscore) | config.js:22-29,452-461 | ported | logic.ts TABS + entriesForTab (prefix match then search filter); logic.test.ts::entriesForTab (exact/dot/underscore match, tab routing). ConfigPage.tsx `.cfg-tabs` role=tablist, active tab renders its FormTab. Live-verified: Core tab active by default, all six tab buttons present |
| Object flattening to dotted-key leaf fields (recurse plain objects to depth 3; arrays/null/empty-object/depth-4 objects stay JSON-blob leaves) | config.js:422-450 | ported | logic.ts flattenEntries (FLATTEN_MAX_DEPTH=3, empty-object + depth-limit leaf handling); logic.test.ts::flattenEntries (scalars/arrays/null leaves, depth-3 flatten, depth-4 blob, empty-object leaf). Exercised via entriesForTab + the render tests |
| Field grouping: dotted keys under their top segment, bare scalars under General, bare object under its own id; de-cased group titles; prefix-stripped field labels | config.js:481-510 | ported | logic.ts groupEntries/groupIdForKey/groupTitle/fieldLabel; logic.test.ts::groupEntries (memory/general split, bare-object id); groupTitle (de-case); fieldLabel (prefix strip, general untouched). ConfigPage.tsx FormTab renders `.cfg-group` sections with an N-fields meta. Live-verified: General (5 fields) + Auth (7 fields) groups rendered |
| Field kinds: readonly (host/port, display-only, no data key) / boolean switch / number / object JSON textarea (with Edit summary) / string; sensitive keys (key\|token\|secret\|password\|api_key on the FULL dotted key) → masked password input | config.js:514-565 | ported | logic.ts fieldKind + isReadonlyKey + isSensitiveKey + objectSummary; logic.test.ts::fieldKind (readonly wins, type map); isSensitiveKey (full dotted key); isReadonlyKey (host/port). ConfigPage.tsx ConfigField renders each kind; ConfigPage.test.tsx::renders host/port as read-only (no editable input, no save tracking). Live-verified: host/port plain text, debug/diagnostics switches, version text, token/password masked |
| Dirty-state tracking + no-op short-circuit: a field edit diffs the pending value vs the loaded config (equal by ref OR structural JSON → clears dirty; else records {old,new}); the sticky save bar shows only when dirty | config.js:585-616,667-705 | ported | logic.ts computeDirty (THE diff: ref-or-JSON equality clears, else {old,new}) + dirtyCount; logic.test.ts::computeDirty (change/no-op reset/number/nested-leaf/structural array/type-change/absent-key — every diff edge). ConfigPage.tsx onFieldChange → setDirty; StickyBar shown iff count>0. ConfigPage.test.tsx::sets a field dirty → the sticky save bar appears; no-op reset hides it again. **Live-verified end-to-end: toggling debug false→true raised "1 change pending"; toggling back to the loaded value cleared the bar (no-op short-circuit)** |
| Sticky save bar: "N change(s) pending" + View/Hide diff (per-key old→new, or "loaded config → unsaved draft" in YAML mode) + Discard + Save | config.js:195-210,667-711 | ported (delta) | ConfigPage.tsx StickyBar (role=region "Pending changes", count, diff toggle, Discard, Save) + summariseDiffValue; logic.test.ts::summariseDiffValue (JSON encode + 120-char truncate). ConfigPage.test.tsx exercises the bar via the dirty/save/discard tests. **Live-verified: "1 change pending · View diff" expanded to the debug false→true row.** **Delta:** legacy imperative innerHTML bar → a React component; status color via `.tone-info`→`--tone` (never hardcoded) |
| Inline JSON validation blocks submit: an unparseable object-field edit flags "Invalid JSON" and, in form mode, Save short-circuits with "Fix invalid JSON before saving" (config.patch never fires) | config.js:593-602,639-652,735-738 | ported | logic.ts parseFieldValue ({ok:false} on JSON.parse throw) + hasInvalidJson; logic.test.ts::parseFieldValue (invalid JSON → ok:false); hasInvalidJson. ConfigPage.tsx onFieldChange sets the invalid map + `.cfg-json-error`; onSave gates on hasInvalidJson. ConfigPage.test.tsx::invalid JSON in an object field blocks Save (config.patch never fires) |
| Form-mode Save (patch semantics): no-op guard ("No changes to save") → else config.patch {patches:{dottedKey:newValue}}; success/restartRequired toast; reload | config.js:734-744 | ported (delta) | logic.ts buildPatchPayload (dirty → {patches}); logic.test.ts::buildPatchPayload. ConfigPage.tsx patchMutation → config.patch, no-op short-circuit toast, restartRequired advisory toast, invalidate(['config']). ConfigPage.test.tsx::Save (form mode) calls config.patch with only the dirty dotted keys; a restartRequired patch response toasts the restart advisory; Save with no dirty fields short-circuits with a "No changes to save" toast. **Delta:** legacy `_rpc.call` + manual `_loadData` → useMutation + invalidateQueries |
| YAML-mode Save (apply semantics): config.apply {config_yaml, baseline_yaml} — the edited text + the config.get baseline the operator was shown (server diffs & persists only real edits, restoring echoed runtime overrides); success/restartRequired toast; reload | config.js:722-733 | ported (delta) | logic.ts objToYaml (baseline serialiser) + buildApplyPayload; logic.test.ts::objToYaml (scalars/quoting/empty/nested/list — incl. the legacy trimStart posture); buildApplyPayload. ConfigPage.tsx applyMutation → config.apply with the draft + `objToYaml(config)` baseline. ConfigPage.test.tsx::YAML mode Save calls config.apply with the edited YAML and the loaded baseline. patch-vs-apply are distinct RPCs asserted to never cross-fire. **Delta:** legacy `_rpc.call` + `_loadData` → useMutation + invalidate |
| restartRequired advisory: a truthy restartRequired in the patch/apply response swaps the "saved/applied" toast for the "… Gateway restart required …" wording (info tone) | config.js:732,742 | ported | ConfigPage.tsx patchMutation/applyMutation onSuccess branch on res.restartRequired → toast.info(restart wording) else toast.success. ConfigPage.test.tsx::a restartRequired patch response toasts the restart advisory |
| Reload / Discard: clear all edit state (dirty / invalid-JSON / yaml draft / diff-open) and refetch config.get; Discard hidden when nothing is pending | config.js:224-231,242-251 | ported (delta) | ConfigPage.tsx onDiscardOrReload clears the edit state + invalidateQueries(['config']); the render-phase snapshot reset (keyed on dataUpdatedAt) also blanks edits when a fresh tree arrives. ConfigPage.test.tsx::Discard clears dirty state and reloads config.get. **Delta:** legacy `_dirty={}` + `_loadData()` → state reset + react-query invalidate; the sticky Discard only renders when count>0 (legacy `hidden` attr) |
| Header key search: filters the visible fields within the active tab by key OR value (case-insensitive) | config.js:180-183,234-237,457-460 | ported | logic.ts entriesForTab search filter + searchBlob; logic.test.ts::entriesForTab (search over key AND value); searchBlob (lowercase scalars, JSON objects). ConfigPage.tsx `.cfg-search` input → lowercased search prop. ConfigPage.test.tsx::search filters the visible fields within the active tab |
| Guided setup → navigate('/setup') | config.js:232 | ported (delta) | ConfigPage.tsx Guided-setup Button → useNavigate('/setup'); ConfigPage.test.tsx::Guided setup navigates to /setup. **Delta:** legacy Router.navigate → react-router useNavigate; /setup is still the StubView (setup migration is a later task), a link-only target |
| Per-field help: a help trigger per field surfaces the keyed help copy (generic fallback for unknown keys) on hover/focus | config.js:32-130,747-810 | ported (delta) | logic.ts helpFor + the HELP map ported verbatim; logic.test.ts::helpFor (known key, generic fallback). ConfigPage.tsx HelpTrigger renders the copy inline (role=tooltip) revealed via CSS `:hover`/`:focus-visible`. **Delta:** the legacy hand-positioned floating tooltip layer (getBoundingClientRect placement, doc-click/Escape dismissers, hover-into-tooltip grace) is replaced by a CSS-revealed inline tooltip — same copy + keyboard/hover reveal, without the imperative positioning engine (kept simple; no floating-layer library per brief). Flagged for owner review if precise viewport-flip placement is required |
| Object-summary + preview redaction: collapsed object/array fields show a "JSON · N keys/items · {…}" summary with secret-looking keys redacted to "\*\*\*" | config.js:840-874 | ported | logic.ts objectSummary + formatPreviewValue (SECRET_KEY_RE redaction, string truncation); logic.test.ts::objectSummary (array/object preview, empty, api_key redaction). ConfigPage.tsx ConfigField object kind renders `.cfg-object__summary` |
| Document title "Config - AgentOS Control" | js/router.js meta.title | ported | ConfigPage.tsx useEffect document.title; ConfigPage.test.tsx::sets the document title. Live-verified: tab title "Config - AgentOS Control" |
| Dangerous-change confirmation before apply | brief "Notable" list | waived (not in legacy) | The brief's "Notable" list names a "dangerous-change confirmation", but the legacy config.js has **no** confirm dialog, `window.confirm`, or beforeunload/navigation guard anywhere (grep config.js for confirm/Confirm/danger/beforeunload → only help-copy prose hits; the sole safety wiring is dirty tracking + the restartRequired advisory toast). Fabricating a confirm gate the legacy never had would itself break parity, so this is recorded as a real waiver for owner adjudication rather than silently invented or dropped. The guards legacy DOES have (dirty tracking, no-op short-circuit, invalid-JSON-blocks-submit, Discard/Reload reset) are all ported (rows above). **If the owner wants a confirm-before-apply on security-sensitive keys (e.g. debug / auth.allow_unauthenticated_public / control_ui.allowed_origins), it is a NEW behavior to spec, not a port** |
| Unsaved-navigation warning (leaving the view with pending edits) | — (not in legacy) | waived (not in legacy) | The legacy view has no beforeunload handler and no in-app navigation guard: switching views drops pending edits silently (the SPA simply destroys the view). The port matches this — no navigation guard added. Recorded explicitly so the absence is a documented parity decision, not an oversight; a guard would be a new behavior to spec |
| View CSS (cfg-* classes) rebuilt on the shared token system | css/views/config.css | ported (restyled) | views/config/config.css on the globals.css tokens: header hero + AsciiField backdrop (Health posture), Form/YAML mono toggle, tab bar + search, hairline settings groups, tokenized inputs (text/number/masked/JSON-details/switch), CSS-revealed help tooltips, a sticky save bar with a `.tone-info`→`--tone` rail + strike-through/ok diff rows, dashed empty state; status color only via `--tone`, lime signal-only |
| Live parity: /config vs legacy /control/config | config.js (whole view) | RTL (full) + live smoke (partial) | Behaviors covered by **13 ConfigPage RTL + 48 config-logic unit tests** (the authoritative coverage). A **partial live smoke** ran in-browser against the worktree gateway on :18999 (vite dev :5173, /control/config): the Form view rendered the real config.get tree (Connected pill, header hero, Core tab, General + Auth groups, host/port read-only, masked token/password), the **dirty-state + no-op short-circuit** drove end-to-end (debug toggle raised then cleared "1 change pending"), the **diff view** expanded to the debug false→true row, and **YAML mode** rendered the full serialised baseline. ZERO console errors. **NOT driven live:** an actual config.patch / config.apply write (avoided mutating the running gateway), the invalid-JSON gate, the search filter, and the per-field help hover — those are covered by RTL only. A full write + side-by-side vs legacy /control/config is deferred to the cutover manual pass |

### skills
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| Initial load: skills.list after waitForConnection → installed skills; failure → error toast | skills.js:325-340 | ported (delta) | SkillsPage.tsx skillsQuery (waitForConnection → skills.list, data.skills ?? []); load-failure branch → toast.error('Failed to load skills: …', stable id 'skills-load-err'). SkillsPage.test.tsx::calls skills.list after waitForConnection…; toasts when skills.list fails. **Delta:** legacy `_loadData` promise → a react-query useQuery read (same order) |
| Status metric pills: All / Ready / Needs setup / No manifest counts, each toggling a status filter | skills.js:342-367 | ported | logic.ts skillStats (total/ready(=='ready')/needs(=='needs_setup')/notDeclared(=='not_declared')); logic.test.ts::skillStats. SkillsPage.tsx MetricPill row (aria-pressed, `--tone`: accent/ok/warn/dim); SkillsPage.test.tsx::renders the status metric pills from the payload; the status pill filters the installed list |
| Installed filter: free-text over name/description/triggers (case-insensitive) + active status pill; empty-state message per filter/status | skills.js:369-405 | ported | logic.ts filterSkills (name/description/triggers lowercase includes, then status map needs-setup→needs_setup, not-declared→not_declared) + installedEmptyMessage; logic.test.ts::filterSkills (name/description/triggers, status filter, combined); installedEmptyMessage (filter/status/default). SkillsPage.tsx header search input (aria "Filter installed skills") + InstalledPanel; SkillsPage.test.tsx::the header filter searches name/description; the status pill filters the installed list |
| Layer grouping + ready-first sort: bucket by layer (unknown→extra), sort each ready(0)<not_declared(1)<needs_setup(2) then name-asc, emit in LAYER_ORDER (workspace/bundled/managed/personal/project/extra), skip empties; collapsible group with count + help meta | skills.js:36-52,407-442 | ported | logic.ts LAYER_ORDER/LAYER_LABEL/LAYER_HELP + skillRank + groupSkillsByLayer + layerLabel/layerHelp; logic.test.ts::skillRank; groupSkillsByLayer (LAYER_ORDER, ready-first+name sort, extra fallback, empties dropped); layerLabel/layerHelp (known + fallback). SkillsPage.tsx InstalledPanel `<details class=sk-group>` per group; SkillsPage.test.tsx::…renders installed cards grouped by layer (Bundled + Managed) |
| Installed card: status dot class (ready/needs/unverified) + tooltip, emoji, name, clamped description; opens the detail dialog | skills.js:447-465 | ported | logic.ts skillStatus (status ?? eligible) + skillDotClass + skillDotTitle; logic.test.ts::skillStatus/skillDotClass/skillDotTitle. SkillsPage.tsx SkillCard (aria-label `Skill <name>`, dot class, click → skill dialog) |
| Install (catalog/registry): skills.install {identifier, source (default clawhub), force}; success → toast + reload + mark installed; **per-item busy** (button-scoped, NOT global) | skills.js:926-950,979 | ported (delta) | logic.ts installSource (default clawhub) + installAction + registryKey + markInstalled; logic.test.ts cover each. SkillsPage.tsx installMutation → skills.install with onMutate/onSettled toggling a per-identifier busy key (busyKeys Set), onSuccess invalidate(['skills']) + invalidate(['skills.search']). SkillsPage.test.tsx::installing a catalog skill calls skills.install with identifier/source/force and reloads. **Per-item-busy lesson from channels applied: busy is a Set of keys, never one global flag.** **Delta:** legacy manual button.disabled/textContent mutation + `_markInstalled` + `_loadData` → useMutation per-item busy + react-query invalidate |
| Install security-block → force-install arm: a `scan_verdict==='dangerous'` response is a deliberate block (not a crash) — arm an explicit force-install override (danger button); second click sends force:true; the armed state survives re-renders | skills.js:952-971 | ported | logic.ts installAction ('force' when the key is in the forceArmed Set); logic.test.ts::installAction (force-armed). SkillsPage.tsx installMutation onSuccess: `!res.success && scan_verdict==='dangerous' && !force` → armForce(key,true) + error toast "…Click again to install anyway."; the InstallButton renders a destructive "⚠ Force install"; forceArmed lives in page state (survives re-render). SkillsPage.test.tsx::a dangerous scan verdict arms a force install instead of erroring (2nd click sends force:true) |
| GitHub-URL install (Community tab): skills.install {identifier:<url>, source:'github', force:false} on button click or Enter | skills.js:223-232,226 | ported | SkillsPage.tsx `.sk-github-install` url input + button → installMutation.mutate({identifier: url.trim(), source:'github', force:false}); Enter key wired. (Covered by the install RPC assertion path; not a separate RTL case — same mutation) |
| Uninstall (managed skills only): skills.uninstall {name}; success → reload + close dialog; **per-item busy** | skills.js:979-991 | ported (delta) | SkillsPage.tsx uninstallMutation → skills.uninstall {name}, per-item busy key 'uninstall:<name>', onSuccess invalidate(['skills']) + close dialog. SkillsPage.test.tsx::a managed skill dialog removes via skills.uninstall and closes; bundled (non-managed) skill dialog has no Update/Remove. **Delta:** manual button mutation + `_markInstalled` + `_loadData` → useMutation + invalidate |
| Update (managed skills only): skills.update {name} → results[] array, take first; success → toast + reload, else re-enable + error toast; **per-item busy** | skills.js:993-1010 | ported (delta) | logic.ts firstUpdateResult (results[0] ?? {}); logic.test.ts::firstUpdateResult. SkillsPage.tsx updateMutation → skills.update {name}, per-item busy 'update:<name>', onSuccess firstUpdateResult → toast + invalidate(['skills']). SkillsPage.test.tsx::a managed skill dialog updates via skills.update and reloads. **Delta:** manual button mutation + `_loadData` → useMutation + invalidate |
| Dependency install (deps.install): skills.deps.install {name, install_id} from the detail dialog's Install rows (shown iff missing_bins non-empty); success toast; when nothing is still missing → close the dialog; always reload; **per-item busy** | skills.js:807-823,879-908 | ported (delta) | logic.ts stillMissingCount (missing_still.bins+env); logic.test.ts::stillMissingCount. SkillsPage.tsx SkillDialog Install section (installs = missing_bins.length ? skill.install : []), each row → depsMutation.mutate({name, installId}) with per-item busy 'deps:<name>:<id>'; onSuccess: toast, `stillMissingCount(res)===0` → close dialog, always invalidate(['skills']). SkillsPage.test.tsx::the install-deps button…calls skills.deps.install with name + install_id (nothing-missing → closes); deps.install that leaves deps missing keeps the dialog open. **Delta:** legacy manual button mutation + `_loadData` → useMutation + invalidate |
| Registry browse (Bankr / Community): skills.search on tab entry — Bankr {query:'', limit:500, source:'bankr'}; Community {query:'', limit:500} then communityFilter (drop source==='bankr' while the Bankr tab shows) | skills.js:503-526 | ported (delta) | logic.ts communityFilter (SHOW_BANKR ? drop bankr : keep); logic.test.ts::communityFilter (both branches). SkillsPage.tsx bankrSnapshot/communitySnapshot useQuery (enabled on the active tab, queryKey ['skills.search', group]) → skills.search with the exact params. **Delta:** legacy `_browse` + `_registryCache`/`_registryError`/`_registryLoading` state machine → two react-query reads (react-query owns cache/error/loading + the "retry on re-entry" via enabled+staleness) |
| Community server-side search (debounced): a non-empty query hits skills.search {query, limit:100} (the snapshot only covers each source's first page), **debounced at the legacy 250ms**; a cleared input drops the query back to the snapshot; stale responses never win | skills.js:208-221,528-545 | ported (delta) | logic.ts REGISTRY_SEARCH_DEBOUNCE_MS (=250, asserted); logic.test.ts::REGISTRY_SEARCH_DEBOUNCE_MS. SkillsPage.tsx a 250ms setTimeout debounce (communityText → communityQuery) drives a communitySearch useQuery (enabled when communityQuery.length>0, queryKey includes the query text so react-query drops stale responses); a cleared input → communityQuery '' → snapshot shows. SkillsPage.test.tsx::community search fires skills.search once after the debounce interval (3 quick keystrokes → exactly ONE skills.search for the settled value 'uni'; the 'u'/'un' intermediates never reach the server). **Delta:** legacy `_debounce`+`_communitySeq` sequence-drop → setTimeout debounce + react-query query-key staleness |
| Category chips + registry filter: chips derive from the FULL snapshot only (never change on keystrokes), 'all' first then count-desc, hidden when only 'other'; results filtered by active category then case-insensitive text (name/provider/description); empty message per group/query | skills.js:560-631 | ported | logic.ts categoriesFor + categoryChips (snapshot-only, all-first count-desc, other-only hidden) + filterRegistry (category then text) + registryEmptyMessage + CAT_LABEL; logic.test.ts cover each (chips ordering/active/other-only/empty; filterRegistry category+text+combined; registryEmptyMessage). SkillsPage.tsx RegistryPanel chips from `chipSnapshot` (the full snapshot, distinct from the search-narrowed base list) + filterRegistry over the base list |
| Registry card + detail dialog: logo/emoji/initials badge (http(s)-only logo, onerror→initials), install action, provider/source; dialog shows trust chip, source, description (or "loads after install"), setup steps, demo code, homepage (Source ↗), and the install action | skills.js:643-739 | ported | logic.ts initials + safeUrl + installAction + registryKey; logic.test.ts::initials (first two words); safeUrl (http(s) only, blocks javascript:); installAction. SkillsPage.tsx LogoBadge (safeUrl gate + onError→initials, emoji fallback), RegistryCard, RegistryDialog. Keyboard-activatable card (Enter/Space, role=button) preserved |
| Robinhood partner tab: installed Robinhood-family skills — **bundled only** whose name starts robinhood* or homepage mentions robinhood.com (a community skill can't wear the partner banner); name-sorted; empty → "on the way" state | skills.js:467-497 | ported | logic.ts isRobinhoodSkill (layer==='bundled' guard + name/homepage match) + robinhoodSkills (filter+name-sort); logic.test.ts::isRobinhoodSkill (bundled-only guard rejects a managed robinhood-* skill); robinhoodSkills. SkillsPage.tsx RobinhoodPanel; SkillsPage.test.tsx::the Robinhood tab shows the empty state when no robinhood skills are installed. **Security-relevant guard ported faithfully — the bundled-only restriction is the brand-spoofing defense, not dropped** |
| Refresh button: Bankr/Community re-fetch their snapshot; Installed/Robinhood reload skills.list | skills.js:190-201 | ported (delta) | SkillsPage.tsx refresh(): tab==='bankr'→bankrSnapshot.refetch(); community→communitySnapshot.refetch()(+communitySearch if a query is active); else invalidate(['skills']). **Delta:** legacy `_registryCache[tab]=null; _browse()` / `_loadData()` → react-query refetch/invalidate |
| Registry cache retry-on-re-entry: a failed catalog load leaves the cache empty so re-entering the tab retries automatically | skills.js:309-312,519-521 | ported (delta) | SkillsPage.tsx the snapshot useQuery is `enabled` only on the active tab; a failed fetch is not cached as data, so re-selecting the tab re-runs the queryFn (react-query's enabled+retry:false posture reproduces the legacy null-cache retry). **Delta:** explicit null-cache sentinel → react-query enabled-gating |
| CLI command line (`$ …` via common `<CommandLine>`) | skills.js (whole view) | ported (n/a) | The legacy skills view renders **no** `$ …` command line — grep skills.js for `cmdline`/`CommandLine`/`command`/`$ ` → the only shell-ish surface is the GitHub-URL install input (a URL field, not a copyable CLI hint) and the demo `<pre><code>` block (arbitrary catalog-supplied code, shown read-only in the dialog, not a control command). So the shared `<CommandLine>` requirement has no applicable control in this view. No deviation — the control simply does not exist in the legacy source |
| Document title "Skills - AgentOS Control" | js/router.js meta.title | ported | SkillsPage.tsx useEffect document.title; SkillsPage.test.tsx::sets the document title |
| "Dependency-install prompt/confirmation" (brief Notable list) | brief "Notable" list; skills.js:807-823,879-908 | ported (as-in-legacy) + confirm waived | The brief's Notable list phrases this as a "prompt/confirmation". In legacy skills.js the dependency install is a **direct button** inside the detail dialog (`data-install-deps-name`) whose handler `_installDeps` fires skills.deps.install **immediately** on click — there is NO `window.confirm`, no second-step confirmation dialog anywhere in skills.js (grep confirm/Confirm → zero hits). The "prompt" IS the dialog's Install row + button, which is ported faithfully (per-item busy, still-missing→close, reload). A separate confirm-before-deps-install gate is **waived (not in legacy)** — adding one would break parity; it would be a NEW behavior to spec if the owner wants a guard on dependency installs |
| Bankr partner tab visibility flag (_SHOW_BANKR) | skills.js:7 | ported | SkillsPage.tsx SHOW_BANKR=true (matches legacy `_SHOW_BANKR=true`); the Bankr tab renders and communityFilter drops bankr rows from Community while it shows. The flag is a module const in both, flipping tab visibility while the BankrSource backend stays wired |
| View CSS (sk-* classes) rebuilt on the shared token system | css/views/skills.css | ported (restyled) | views/skills/skills.css on the globals.css tokens: header hero + AsciiField backdrop (Health posture), status metric pills with a per-tone left gutter (`--tone`: accent/ok/warn/dim), tab bar (lime active underline — signal-only), hairline collapsible layer groups, card grids, registry cards with logo/initials badges, category chips, a tokenized modal (overlay + Escape/backdrop close) for both detail dialogs, status chips via `--tone`; lime `--primary` signal-only (active tab/nav/focus), never a status color |
| Live parity: /skills vs legacy /control/skills | skills.js (whole view) | RTL (full) + live pending | Behaviors covered by **15 SkillsPage RTL + 35 skills-logic unit tests** (the authoritative coverage) + a clean production `npm run build` (the view compiles into the real bundle). **Live smoke NOT run:** the React dev server (vite :5173) was not confirmed serving a React build during this task, and the :18999 gateway serves the LEGACY static console, so it cannot exercise the new SkillsPage. A live in-browser pass on :5173 + side-by-side vs legacy /control/skills is deferred to the cutover manual pass |

### cron
| behavior | legacy source | status | evidence / reason |
| --- | --- | --- | --- |
| cron.list {} after waitForConnection; tolerate a bare array OR {jobs:[…]} | cron.js:339-347 | ported | CronPage.tsx jobsQuery (waitForConnection → cron.list, Array.isArray ? data : data.jobs); CronPage.test.tsx::calls cron.list after waitForConnection |
| **Subscribe/unsubscribe lifecycle: cron.subscribe on mount (after waitForConnection; best-effort — a pre-connect call rejects "Not connected"), cron.unsubscribe on unmount** | cron.js:306-316,318-321 | ported | CronPage.tsx useEffect (waitForConnection→cron.subscribe; cleanup→cron.unsubscribe; `cancelled` guard so a resolve after unmount doesn't re-subscribe); CronPage.test.tsx::mounts → cron.subscribe; unmounts → cron.unsubscribe …; **subscribes exactly once across a StrictMode-style double effect and cleans up fully** (mount→unmount→mount, listener count returns to 0 each cleanup, no leak across remounts) |
| **Real-time `cron.run.finished` event → invalidate runs (job list + any open run-history drawer), cleaned up on unmount** | cron.js:313-315,349-353 | ported (delta) | CronPage.tsx useEffect rpc.on('cron.run.finished', …) → invalidateQueries(['cron']) with unsub cleanup; the runs drawer query key `['cron','runs',<id>]` is prefixed by `['cron']` so a single invalidate refetches both; CronPage.test.tsx::a cron.run.finished event invalidates the job list (targeted refetch); a cron.run.finished event invalidates an open runs drawer. **Delta:** legacy `_scheduleCronReload` did a full `_loadData` + a 750ms debounced second reload (cron.js:349-353); the port issues a single targeted react-query invalidate (react-query already coalesces concurrent refetches, so the debounce is unnecessary) |
| Summary stats: Active schedules (enabled / total, +paused hint), Upcoming runs, Reminders, Agent tasks | cron.js:381-431 | ported (delta) | logic.ts isUpcomingRun; CronPage.tsx cron-stats (enabledCount/paused/upcoming/reminders/agentTasks) + StatTile; CronPage.test.tsx::renders the stat row from the payload. **Delta:** legacy's "Next run" live-countdown tile + "Last 24h runs ok/fail" aggregate tile are replaced by Upcoming-runs / Reminders / Agent-tasks tiles (no per-second countdown tile — see the 1Hz-tick waiver); the reminder/agent mix is surfaced as two explicit tiles instead of the legacy combined chip |
| isUpcomingRun(job): enabled, not running, next_run parses to the future | cron.js:355-360 | ported | logic.ts isUpcomingRun; logic.test.ts::isUpcomingRun (future→true; disabled/running/missing/past/invalid→false) |
| nextRunText: — when disabled/absent/invalid, "running" while running, "awaiting update" when the timestamp is already past, else a human countdown | cron.js:362-370 | ported | logic.ts nextRunText (clock injected); logic.test.ts::nextRunText (all branches, "in 5m 0s") |
| nextRunAbs: '' when disabled/running/absent/past, else a friendly today/tomorrow/date+time | cron.js:372-377 | ported | logic.ts nextRunAbs + humanTime; logic.test.ts::nextRunAbs; humanTime (today/tomorrow/dated) |
| Job kind label (Reminder / System event / Agent task) + class (is-reminder / is-agent); target + schedule display (camel|snake fallbacks) | cron.js:600-622 | ported | logic.ts jobKindLabel/jobKindClass/jobTarget/jobSchedule; logic.test.ts covers each (camel/snake, — fallbacks); CronPage.tsx JobCard pill/target/schedule; CronPage.test.tsx::renders a card per job with kind pill and schedule |
| Status dot variant: off (disabled) / error (last_status error|fail) / on (incl. inferred ok from last_run); status color ONLY via --tone | cron.js:614,624,686 | ported | logic.ts jobDotState + isOkStatus; logic.test.ts::jobDotState (off/error/on incl. inferred); isOkStatus (ok/success only); CronPage.tsx dotTone maps the bucket → tone-danger/dim/ok (never hardcoded) |
| Job card: dot, name (opens run history), kind pill, schedule `<code>` + explainCron humanization, Target / Last run (+status) / Next run (countdown +abs) / Prompt (truncated 140) meta, action row | cron.js:612-650 | ported (delta) | CronPage.tsx JobCard; CronPage.test.tsx::renders a card per job …. **Delta:** legacy `_esc` HTML-escaping is obsolete (React escapes text nodes); the legacy Cards/Table view-mode toggle is not ported (cards only — see the table-view waiver) |
| Enable/disable toggle → cron.update {id, enabled:!enabled}; success info toast + reload; failure toast | cron.js:735-744 | ported | CronPage.tsx toggleMutation → invalidateQueries(['cron']); CronPage.test.tsx::toggling enable/disable calls cron.update and invalidates |
| Run-now → cron.run {id}; toast reply(120-char)/error/triggered; failure toast | cron.js:746-766 | ported (delta) | CronPage.tsx runMutation (res.reply→success / res.error→warning / else "Job triggered"); CronPage.test.tsx::run-now calls cron.run. **Delta:** the legacy per-button "Running…" spinner relabel is replaced by disabling the card action row while any mutation is pending (mutation.isPending → busy) |
| Run-history drawer: name click toggles selection → cron.runs {id, limit:10}; table (Time relTime / Status tone / Duration ms / Delivery obj-or-string / Reply 120 / → Chat link); empty + failure notes | cron.js:863-923 | ported (delta) | logic.ts runRow (deliveryStatus object→"ch:…, ws:…" / string / —; reply 120; statusOk); logic.test.ts::runRow (object/string/absent, missing fields); CronPage.tsx RunsDrawer (useQuery ['cron','runs',id], → Chat → navigate('/chat?session=')); CronPage.test.tsx::opening the runs drawer calls cron.runs and renders history. **Delta:** legacy imperative fetch-into-detail-panel → a nested react-query read keyed by the selected id; Router.navigate → react-router useNavigate |
| Remove: confirmation modal ("Delete <name>? This cannot be undone.") → cron.remove {id}; clears selection if the deleted job was open; success info toast + reload; failure toast | cron.js:768-788 | ported (delta) | CronPage.tsx DeleteConfirm (role="alertdialog", Escape/backdrop close, destructive Delete) → removeMutation → invalidateQueries; CronPage.test.tsx::deleting requires confirmation then calls cron.remove and invalidates; cancelling the delete confirmation does not call cron.remove. **Delta:** legacy UI.modal MutationObserver confirm → tokenized alertdialog |
| Search filter (name / message|prompt / payloadKind / sessionTarget|session_target / expression|schedule, case-insensitive) + sort (name/payloadKind/sessionTarget/expression/last_run/next_run; date cols numeric, missing→ends; **asc/desc toggle**) | cron.js:539-570 | ported (delta) | logic.ts filterJobs + sortJobs; logic.test.ts::filterJobs (5-field match, copy on empty); sortJobs (string/date, missing→ends, no-mutate); CronPage.tsx header search input + sort `<select>` + an **asc/desc direction toggle button** (default next_run asc); CronPage.test.tsx::toggles the sort direction and reorders the cards. **Delta:** the legacy per-column sortable **table headers** become a sort-column `<select>` + a direction toggle (the asc/desc capability the review flagged is restored); the search box moves into the header actions |
| Cron parser + next-run computation: parseField (list/range/step/names), parseCron (5-field, 7→0 fold, null on malformed), nextRuns (Vixie DOM/DOW, minute scan to +1yr) | cron.js:1255-1343 | ported | logic.ts parseField/parseCron/nextRuns; logic.test.ts::parseField (wildcard/list/range/step/names); parseCron (null on non-5-field; Sunday via 0; **legacy out-of-range guard documented — a bare `7` (>max 6) yields an empty dow set, so the 7→0 fold at cron.js:1304 is dead code kept for 1:1 fidelity**); nextRuns (N increasing future times; [] on unparseable) |
| Cron humanizer: explainCron (every minute / hourly at :mm / daily|weekday|weekend|date|month at HH:MM / every N minutes / at-minute-hour fallback) | cron.js:1345-1399 | ported | logic.ts explainCron + humanizeFieldList; logic.test.ts::explainCron (all common patterns + '' on unparseable + fallback) |
| Time/duration formatters: formatDuration (s/m/h/d), humanCountdown (in/now/ago), humanCountdownPast (just now/ago/in), humanTime (today/tomorrow/dated) | cron.js:1446-1483 | ported | logic.ts formatDuration/humanCountdown/humanCountdownPast/humanTime (clock injected); logic.test.ts covers each |
| Refresh button reloads jobs | cron.js:265,39-41 | ported (delta) | CronPage.tsx Refresh → queryClient.invalidateQueries(['cron']); CronPage.test.tsx::refreshes on the Refresh button. **Delta:** legacy `#cron-refresh` → react-query invalidate |
| Empty state: no jobs ("No schedules yet." + "Create your first schedule" CTA + three preset quick-start hints) vs no search matches ("No matches") | cron.js:793-846 | ported (delta) | CronPage.tsx cron-empty (CTA → create panel; EMPTY_TEMPLATES hints → create panel pre-seeded with each template) vs cron-empty ("No matches"); CronPage.test.tsx::shows the empty state when there are no jobs; the empty-state CTA opens the create panel. **Delta:** the legacy decorative inline-SVG clock is dropped (token-system empty state, matching the agents/sessions posture); the CTA + the three preset templates are RESTORED and now open the real create panel (pre-seeded) |
| Load-failure toast ("Failed to load cron jobs: …") | cron.js:346 | ported (delta) | CronPage.tsx jobsQuery.isError → toast effect (stable id); CronPage.test.tsx::toasts when cron.list fails. **Delta:** legacy caught in _loadData; the port surfaces via a react-query error branch (same message) |
| Document title "Cron - AgentOS Control" | js/router.js meta.title | ported | CronPage.tsx useEffect document.title; CronPage.test.tsx::sets the document title |
| Toast contract (sonner seam) | components.js UI.toast | ported (delta) | All cron toasts use sonner (success/warning/info/error) with stable per-action ids; the assertive-announce seam limitation recorded on the health copy-toast row applies |
| **Create/Edit schedule panel** — opened by the header "New job", each card's Edit button, and the empty-state CTA/preset hints; tokenized modal (overlay + Escape/backdrop close, name focused on open) for BOTH create → **cron.create** and edit → **cron.update** (full schedule+payload) | cron.js:71-263,927-995 | ported (delta) | CronPanel.tsx (role="dialog", seedForm state, focus #cp-name, Escape/backdrop close); CronPage.tsx PanelState + "New job"/Edit/empty-CTA wiring + saveMutation (build.method → cron.create|cron.update) → invalidateQueries(['cron']); CronPage.test.tsx::New job opens the create panel …calls cron.create and invalidates; edit prefills the form and Save sends the full payload via cron.update {id}; closes the panel on Cancel; the empty-state CTA opens the create panel. **Delta:** legacy slide-in `#cron-panel` + scrim → the shared tokenized modal shell (agents-dialog posture); the card Edit button now opens this real editor (the interim CLI-pointer toast is removed) |
| Panel seeding: create defaults (reminder / isolated / cron / agent main / enabled) or a template seed; edit seed from the job (agent_turn default kind, message from prompt, snake/camel fallbacks, every/at scheduleRaw only for the matching kind) | cron.js:937-966 | ported | logic.ts seedForm; logic.test.ts::seedForm (create defaults + template honoring; edit fallbacks; every/at scheduleRaw gating) |
| Schedule type cron/every/at with live cron explain + up to 3 upcoming runs + the four presets (Every 5m/Hourly/Weekdays 09:00/Sundays midnight) | cron.js:95-120,1401-1442 | ported (delta) | CronPanel.tsx CronExplain (parseCron→explainCron + nextRuns(3)) + PRESETS; logic.ts explainCron/nextRuns already tested. **Delta:** the legacy 60ms debounced preview timer is dropped — the explain/upcoming derive synchronously via useMemo (react re-render is already the debounce) |
| Job mode reminder/agent_turn/system_event → session-target logic: system_event locks main (Event text); reminder locks isolated (Reminder text); agent_turn is user-choice with a stale-main coercion + a named/current session-key row (Task prompt) | cron.js:997-1057 | ported | logic.ts resolveTarget; logic.test.ts::resolveTarget (system_event→locked main; reminder→locked isolated; agent_turn main→current/isolated coercion; session-key row for current/session); CronPanel.tsx renders the locked select + conditional key row + message label; CronPage.test.tsx::agent-task mode reveals the session-target picker and a named-session key requirement |
| Advanced delivery: mode none/announce/webhook (channel/recipient/account or webhook url/token), best-effort toggle; failure-destination channel/webhook; delivery/fd payload builders with per-field validation | cron.js:1059-1178 | ported | logic.ts deliveryFormFromJob + buildDelivery + buildFailureDest; logic.test.ts::deliveryFormFromJob (announce/none/webhook + fd, no token echo); buildFailureDest (webhook url required; channel needs channel-or-recipient; lowercases channel); buildDelivery (none/webhook/announce, nested fd, propagated fd error, standalone fd patch); CronPanel.tsx advanced `<details>` with conditional delivery/fd rows |
| Save assembly + every legacy guard: name required; interval a positive integer; ISO time required for 'at'; tz stamps payload + cron schedule; wakeMode only when ≠now; current/named session-key required + three-way binding; reminder origin binding; system_event forces main; edit carries {id} → cron.update | cron.js:1180-1251 | ported (delta) | logic.ts buildSavePayload (returns {ok,method,payload} | {ok:false,error}); logic.test.ts::buildSavePayload (name required; create cron.create + reminder origin; edit cron.update {id}; every positive-int; at ISO required; tz stamps schedule; wakeMode gate; agent_turn current three-way bind + missing-key error; agent_turn session named-key required; system_event forces main; delivery-error propagation); CronPanel.tsx submit → validation warn toast + inline error, else onSubmit; CronPage.test.tsx::blocks submit and toasts when the name is blank; switching schedule type to interval validates a positive integer. **Delta:** legacy per-guard UI.toast('..','warn') → a single sonner warn toast (stable id) + an inline role="alert" error; native form constraints are disabled (noValidate) so JS validation stays the single source of truth (legacy parity) |
| Session-key helpers: canonicalSessionKey (default/webchat/sess- aliases), jobSessionKey (field-alias precedence), activeChatSessionKey (URL ?session= then agentos_active_session localStorage) | cron.js:1485-1520 | ported (delta) | logic.ts canonicalSessionKey/jobSessionKey/activeChatSessionKey (pure — URL+storage injected); logic.test.ts covers each; CronPage.tsx readActiveSessionKey reads window.location.search + localStorage at the edge and passes them to the pure helper. **Delta:** the impure DOM/storage reads move to a thin edge wrapper so the transform stays unit-tested |
| Horizon ribbon (next-12h marker rail + hour axis, marker click scrolls to card, live drift) | cron.js:460-535 | waived (design) | The decorative next-12h horizon ribbon is not ported — it is a visualization layer (not a data/behavior surface): the same "what runs next" information is carried by the Upcoming-runs stat tile + each card's Next-run countdown. No RPC, event, or mutation is lost. Deferred to a later polish task if the ribbon is wanted on the token system |
| 1Hz tick: live countdown refresh + imminent-row (<60s) pulse + horizon drift | cron.js:303-304,434-456 | waived (design) | The per-second setInterval that re-rendered countdowns / pulsed imminent rows is not ported. Countdowns are computed at render time (nextRunText) and refresh on the next query settle / cron.run.finished invalidate; there is no per-second re-render loop. No data behavior is lost — only the live-ticking animation. This also removes the legacy `_intervals` teardown concern. Deferred to a polish task if live ticking is wanted |
| Cards/Table view-mode toggle + the alternate dense table rendering | cron.js:62-65,289-299,586-590,652-706 | waived (design) | Only the **card** layout is ported (the richer of the two legacy layouts). The Cards/Table segmented toggle and the alternate `<table>` rendering are dropped; ALL sort/filter *capability* is preserved on the card view — the six legacy sort columns via the header sort-`<select>`, the **asc/desc direction toggle** (restored, see the search/sort row), and search via the header search box — so nothing but the alternate table *rendering* is lost. Deferred to a polish task if the dense table view is wanted |
| View CSS (cron-* classes) rebuilt on the shared token system | css/views/cron.css | ported (restyled) | views/cron/cron.css on the globals.css tokens: header hero + AsciiField backdrop (Health posture), hairline stat tiles with a lime hero gutter, .panel job cards with a status --tone gutter+dot (off/error/on → tone-dim/danger/ok, never hardcoded), kind pill, run-history drawer table, dashed empty state, tokenized destructive delete alertdialog (overlay + Escape/backdrop close); status color only via --tone, lime `--primary` signal-only |
| Live parity: /cron vs legacy /control/cron | cron.js (whole view) | RTL (full) + live pending | Behaviors covered by **25 CronPage RTL + 71 cron-logic unit tests** (the authoritative coverage; incl. the full create/edit panel: create→cron.create, edit→cron.update with the full payload, every validation guard, session-target logic, delivery/failure-dest builders) + a clean `npm run check` (tsc + eslint + prettier + 711 tests) + a clean production `npm run build`. **Live smoke NOT run:** the React dev server (vite :5173) was not confirmed serving a React build during this task, and the :18999 gateway serves the LEGACY static console, so it cannot exercise the new CronPage. The highest-risk surfaces — the subscribe(mount)/unsubscribe(unmount) lifecycle + cron.run.finished→invalidate, and the create/edit payload assembly — are covered by explicit RTL/unit tests (incl. a StrictMode-style mount→unmount→mount no-leak assertion). A live in-browser pass on :5173 + side-by-side vs legacy /control/cron is deferred to the cutover manual pass |

## Mechanical inventory (generated by scripts/fe_parity_inventory.py)

### RPC methods (58)

| method | legacy sources |
| --- | --- |
| `agents.create` | static/js/views/agents.js, static/js/views/sessions.js |
| `agents.delete` | static/js/views/agents.js |
| `agents.list` | static/js/views/agents.js, static/js/views/sessions.js |
| `agents.update` | static/js/views/agents.js |
| `channels.access.list` | static/js/views/channels.js |
| `channels.access.resolve` | static/js/views/channels.js |
| `channels.access.revoke` | static/js/views/channels.js |
| `channels.access.setMode` | static/js/views/channels.js |
| `channels.status` | static/js/views/channels.js, static/js/views/setup.js |
| `chat.abort` | static/js/views/chat.js |
| `chat.history` | static/js/views/chat.js |
| `chat.send` | static/js/views/chat.js |
| `commands.list_for_surface` | static/js/views/chat.js |
| `config.apply` | static/js/views/config.js |
| `config.get` | static/js/views/approvals.js, static/js/views/chat.js, static/js/views/config.js, static/js/views/setup.js |
| `config.patch` | static/js/views/config.js, static/js/views/setup.js |
| `config.patch.safe` | static/js/views/chat.js |
| `cron.create` | static/js/views/cron.js (rpc_cron.py:642 — alias of cron.add; the generator missed this alias, added on the Task-11 fix pass) |
| `cron.list` | static/js/views/cron.js |
| `cron.remove` | static/js/views/cron.js |
| `cron.run` | static/js/views/cron.js |
| `cron.runs` | static/js/views/cron.js |
| `cron.subscribe` | static/js/views/cron.js |
| `cron.unsubscribe` | static/js/views/cron.js |
| `cron.update` | static/js/views/cron.js |
| `doctor.memory.status` | static/js/views/setup.js |
| `doctor.status` | static/js/views/health.js, static/js/views/overview.js |
| `logs.status` | static/js/views/logs.js |
| `logs.tail` | static/js/views/logs.js |
| `models.list` | static/js/views/chat.js |
| `onboarding.audio.configure` | static/js/views/setup.js |
| `onboarding.catalog` | static/js/views/setup.js |
| `onboarding.channel.probe` | static/js/views/setup.js |
| `onboarding.channel.upsert` | static/js/views/setup.js |
| `onboarding.imageGeneration.configure` | static/js/views/setup.js |
| `onboarding.memory_embedding.configure` | static/js/views/setup.js |
| `onboarding.provider.configure` | static/js/views/setup.js |
| `onboarding.router.configure` | static/js/views/setup.js |
| `onboarding.search.configure` | static/js/views/setup.js |
| `onboarding.status` | static/js/views/setup.js |
| `router.hold.clear` | static/js/views/chat.js |
| `router.hold.set` | static/js/views/chat.js |
| `sessions.contextCompact` | static/js/views/chat.js |
| `sessions.create` | static/js/views/sessions.js |
| `sessions.delete` | static/js/views/sessions.js |
| `sessions.list` | static/js/views/overview.js, static/js/views/sessions.js |
| `sessions.messages.subscribe` | static/js/views/chat.js |
| `sessions.messages.unsubscribe` | static/js/views/chat.js |
| `sessions.reset` | static/js/views/chat.js |
| `skills.deps.install` | static/js/views/skills.js |
| `skills.install` | static/js/views/skills.js |
| `skills.list` | static/js/views/skills.js |
| `skills.search` | static/js/views/skills.js |
| `skills.uninstall` | static/js/views/skills.js |
| `skills.update` | static/js/views/skills.js |
| `status` | static/js/views/overview.js |
| `tools.search_provider` | static/js/views/chat.js |
| `usage.status` | static/js/views/chat.js, static/js/views/overview.js, static/js/views/usage.js |

### Routes (13)

- `/agents`
- `/approvals`
- `/channels`
- `/chat`
- `/config`
- `/cron`
- `/health`
- `/logs`
- `/overview`
- `/sessions`
- `/setup`
- `/skills`
- `/usage`

### Storage keys (13)

| key | legacy sources |
| --- | --- |
| `agent:main:webchat:default` | static/js/views/chat.js |
| `agentos-router-fx` | static/js/views/chat.js |
| `agentos-theme` | static/js/theme.js |
| `agentos-usage-range` | static/js/views/usage.js |
| `agentos-widget:` | static/js/views/chat.js |
| `agentos.chat.debug.enabled` | static/js/views/chat.js |
| `agentos.chat.debugLog` | static/js/views/chat.js |
| `agentos.elevatedMode` | static/js/approval_monitor.js, static/js/views/approvals.js, static/js/views/chat.js |
| `agentos.elevatedMode.version` | static/js/approval_monitor.js, static/js/views/approvals.js, static/js/views/chat.js |
| `agentos.savingsFx` | static/js/components/savings-fx.js |
| `agentos.wsToken` | static/js/app.js |
| `agentos.wsUrl` | static/js/app.js |
| `agentos_active_session` | static/js/views/chat.js, static/js/views/cron.js |
